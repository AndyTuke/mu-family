# Issues

**Status key:** ✅ Fixed / resolved &nbsp;|&nbsp; 🔴 Open &nbsp;|&nbsp; 🟡 On Hold (deferred, blocked, or pending external action)

## 🔴 Open

| # | Description | Status | Fixed Build |
|---|---|---|---|

## 🟡 On Hold

| # | Description | Status | Fixed Build |
|---|---|---|---|
| 99 | Code signing — the installer `.exe`, standalone `.exe`, VST3 `.vst3`, and CLAP `.clap` must be signed with an EV (Extended Validation) code signing certificate to pass Windows Defender SmartScreen without user intervention. Steps: (1) purchase EV cert from Sectigo or DigiCert (~£200/yr, delivered on hardware token); (2) install `signtool.exe` (Windows SDK); (3) add `SignTool=` directive to `installer/mu-Clid.iss` pointing at the cert; (4) add a post-build `signtool sign` step in CMakeLists.txt (or a separate signing script) for the VST3/CLAP/Standalone artefacts before the installer step. A placeholder comment is already in `mu-Clid.iss`. Deferred until public release. | 🟡 On Hold | — |

## ✅ Fixed

| # | Description | Status | Fixed Build |
|---|---|---|---|
| 239 | Modulator Loop / Step multiplier label rendered as garbled glyphs ("Ä□ 4" instead of "× 4"). Root cause was the default-member-initialiser in `ModulatorEditor.h` passing `"\xc3\x97"` as `const char*` to NudgeInput's `juce::String` ctor — the implicit conversion was not treating the bytes as UTF-8 in this build configuration. Fix: explicit `juce::String::fromUTF8("\xc3\x97")` for both `loopMult` and `stepMult` so the multiplication-sign codepoint U+00D7 is unambiguously constructed. Builds clean (Debug + Release) at v1.0.356. | ✅ Fixed | 356 |
| 238 | Mixer Output dropdown labels updated to descriptive names. `MixerChannel.cpp` ctor at line 56-58 now adds "Main" for bus 0 (was "M") and "Out 1" … "Out 8" for buses 1–8 (was raw "1" … "8"). Both the closed dropdown's selected-item text and the popup menu items pick this up automatically. No APVTS or behaviour change. Builds clean (Debug + Release) at v1.0.356. | ✅ Fixed | 356 |
| 237 | Modulator state persistence (HIGH). Per-rhythm `controlSequences` and `modulationMatrix.getAssignments()` now serialise to a `Modulators` ValueTree child via new `serialiseModulators(const Rhythm&)` and `deserialiseModulators(const ValueTree&, Rhythm&)` helpers (`PluginProcessor.cpp`). Schema: `<Modulators>` with `<Seq id="csN" mode="…" polarity="…" loopNV/loopMod/loopMult stepNV/stepMod/stepMult>` children containing `<Step v>` and `<Point x y bez hx hy>` elements, plus `<Asgn id src dest depth curve>` entries for matrix assignments. Wired into all four save/load paths: `populateStateTree` / `restoreStateFromTree` (host APVTS state), `saveRhythmPresetToFile` / `applyRhythmPreset` (.muRhyth files), `savePreset` / `loadPreset` (.muclid full presets). Deserialise spins on `Rhythm::modLock` so the audio thread cannot be inside `ModulationMatrix::process()` or `ControlSequence::evaluate()` mid-load. New `clearModulators(Rhythm&)` helper resets CS step/curve data and removes all assignments before each load so successive preset loads don't accumulate. Bezier handle flags (`bez`, `hx`, `hy`) round-trip exactly; modulator assignments now persist across plugin reloads. Forward declarations added at the top of PluginProcessor.cpp so the earlier-defined save/load methods can reference the helpers. **No format-version bump needed** — pre-#237 state has no `<Modulators>` child, so legacy load is a clean no-op (existing in-memory defaults stay). Highest user-visible impact of the 2026-05-11 sequencer audit. Builds clean (Debug + Release) at v1.0.356. | ✅ Fixed | 356 |
| 236 | `ModMatrixPanel` ModLock wrapping (HIGH). Every `ModulationMatrix` mutation in `ModMatrixPanel.cpp` is now wrapped in `lockMods(*rhythm)` / `unlockMods(*rhythm)` — namespace-local helpers added at the top of the file that spin on `Rhythm::modLock` (same pattern as `ModulatorEditor::lockMod`/`unlockMod`). Mutations covered: `addAssignment` in the "+ Add Assignment" handler; `removeAssignment` + `addAssignment` in `onDestChange`; `removeAssignment` in `onRemove`; `setDepth` in `onDepthChange`; `setCurve` in `onCurveChange` (#224). Previously the audio thread could be inside `ModulationMatrix::process()` iterating `cachedSortOrder` while `addAssignment`'s `rebuildCache()` resized the vector — concurrent read-during-resize was undefined. Builds clean (Debug + Release) at v1.0.356. | ✅ Fixed | 356 |
| 235 | `swapRhythms` now holds `rhythmsLock` around the data swap (MEDIUM). `PluginProcessor::swapRhythms` previously relied on `suspendProcessing(true)` alone — but per `PluginProcessor.h:266` that only sets a flag and does NOT block the in-flight `processBlock` callback. Wrapped the entire swap mutation block (`sequencer.swapRhythmSlots`, voice/tail/midi `std::swap`, sidechain re-translation, mixer-channel `std::swap`, env resets) in `{ const juce::ScopedLock sl(rhythmsLock); … }`. The audio thread's `ScopedTryLock` in `processBlock` now bails cleanly (silent block) while we hold the lock — atomic with respect to audio. Matches the existing pattern in `removeRhythm` and `handleAsyncUpdate`. Builds clean (Debug + Release) at v1.0.356. | ✅ Fixed | 356 |
| 234 | Audio-thread allocation in pattern snapshot eliminated (MEDIUM). `SequencerEngine` ctor now calls `safePatterns[i].reserve(256)` and `safeCPatterns[i].reserve(256)` for all 8 slots (256 = the `resetSteps` cap and `getCombinedPattern`'s LCM clamp). The snapshot copy in `processBlock` switched from `=` copy-assignment (which can allocate when capacity < source size) to `assign(begin, end)` which reuses the existing storage — guaranteed no-alloc on the audio thread for any pattern size ≤ 256 steps. Builds clean (Debug + Release) at v1.0.356. | ✅ Fixed | 356 |
| 233 | Negative `beatPos` no longer produces OOB pattern indexing (MEDIUM). `SequencerEngine::processBlock` clamps `globalStep = std::max(0, static_cast<int>(beatPosition / StepLengthBeats))` so hosts emitting negative ppq (pre-roll, REW-past-zero, transport jumps) can't drive `effectiveStep` negative — eliminates the signed-int `%` UB that previously made `pattern[stepIndex]` index a negative offset. Defends against the rare host-bug case too. Builds clean (Debug + Release) at v1.0.356. | ✅ Fixed | 356 |
| 232 | Step-skip on very large blocks fixed (MEDIUM). `SequencerEngine::processBlock` now walks every step crossed between `lastStepIndex[r]` (exclusive) and `stepIndex` (inclusive), so a single huge block (offline render, unusual hosts up to 8192-sample blocks) catches all intermediate hits instead of dropping them silently. Modular forward walk capped at `patLen` to avoid infinite loops on degenerate jumps. `firedMask` stays one bit per rhythm per block (per the audit's recommendation); `accentMask` reflects the last hit's accent state from `safeCPatterns`. Rhythm-loop-wrap detection (`rhythmLoopWrapMask`) fires whenever the walk reaches step 0. First-call path (`prevStep < 0`) preserved: check current step directly, no back-walk. Builds clean (Debug + Release) at v1.0.356. | ✅ Fixed | 356 |
| 231 | `lastEffectiveStep` reset on master-loop change and transport stop→start (MEDIUM). `SequencerEngine::setMasterLoopSteps` now resets `lastEffectiveStep = -1` so a mid-playback loop-length change can't emit a false `masterLoopWrapped` event from the stale value. New `SequencerEngine::resetWrapDetector()` public method; `PluginProcessor::processBlock` detects the `playing` false→true edge (comparing against `sequencerPlaying.get()` from the previous block) and calls it, so a transport stop→start can't trigger a false wrap either. False wraps previously gated hot-swap commits, so this also stops premature staged-swap commits. Builds clean (Debug + Release) at v1.0.356. | ✅ Fixed | 356 |
| 230 | `internalBeatPos` and `internalPlaying` are now `std::atomic<>` (MEDIUM). Previously plain `double` / `bool` written by the audio thread (inside `processBlock`) and read+cleared by the UI thread (`isInternalPlaying`, `toggleInternalPlay`) — racy by the C++ memory model even if x64 aligned 8-byte ops were effectively atomic. Switched to `std::atomic<double> internalBeatPos` and `std::atomic<bool> internalPlaying` in `PluginProcessor.h`; all read/write call sites updated to use `.load(std::memory_order_relaxed)` / `.store(…, std::memory_order_relaxed)`. No other memory is published through them so relaxed ordering is sufficient. `internalBpm` stays plain — message-thread only. Builds clean (Debug + Release) at v1.0.356. | ✅ Fixed | 356 |
| 229 | `ModulatorEditor::setData` now holds `modLock` around `loadFromCS()` (LOW). `loadFromCS()` mutates the live `ControlSequence` in two places: `cs->curvePoints.push_back(...)` when the curve is empty (line 207-208) and `cs->stepValues.resize(...)` via `syncStepValues()` (line 236). Without the lock the audio thread could be inside `ControlSequence::evaluate()` for that same cs at the moment of the resize. Wrapped the call in `lockMod()`/`unlockMod()` — the existing helpers used everywhere else in this file. Builds clean (Debug + Release) at v1.0.356. | ✅ Fixed | 356 |
| 228 | `swapRhythmSlots` no longer leaves `lastStepIndex = -1` (LOW). Replaced the `lastStepIndex[i] = lastStepIndex[j] = -1;` pair in `SequencerEngine::swapRhythmSlots` with `patternUpdated[i] = patternUpdated[j] = true;` — the next `processBlock` then runs through the standard snapshot/absorb path which sets `lastStepIndex[r] = effectiveStep % safePatterns[r].size()`, matching `updatePattern`'s semantics. Eliminates the spurious retroactive first-hit fire after a hot-swap. Builds clean (Debug + Release) at v1.0.356. | ✅ Fixed | 356 |
| 227 | `getStepTypes` Mute-mode insert zone now reports `StepType::InsertPad` instead of `StepType::Empty` (LOW). `HitGenerator.cpp:112` updated to emit the pad type for steps inside `[clampedStart, zoneEnd)` so the ring visually distinguishes a muted zone from genuine empty steps — matches the Pad-mode visual identity. Pre/post-pad Mute-mode overwrites at lines 127-139 already did this; the insert branch was inconsistent. Builds clean (Debug + Release) at v1.0.356. | ✅ Fixed | 356 |
| 226 | `EuclideanPanel` rotate slider range updated to `0..steps-1` per design-sequencer.md (LOW). `EuclideanPanel::updateRangesA/B/C` previously called `setRange(-(steps/2), steps/2, 1)` which hid the upper half of the rotate range. Changed to `setRange(0, jmax(0, steps - 1), 1)` matching the design doc. `HitGenerator::getPattern` already accepts the full range and handles wrap correctly. Builds clean (Debug + Release) at v1.0.356. | ✅ Fixed | 356 |
| 225 | Cubic Bézier evaluator `handleX` documented as deliberately unused (LOW). `ControlSequence::CurvePoint::handleX` retained in the struct for forward-compat and persisted by #237's serialiser, but `evaluateSmooth` (#222) is intentionally 1-D in y-over-chord — a true 2-D evaluator complicates the playhead-vs-time mapping and is not free. Comment added to the struct definition flagging this for anyone authoring presets externally so they don't expect horizontal-handle effects. Decision: keep the field, upgrade to 2-D later if user demand emerges. Builds clean (Debug + Release) at v1.0.356. | ✅ Fixed | 356 |
| 224 | Source-side curve on assignments — implemented Bitwig-style per-assignment bipolar curve knob. New `curve` field on `ModulationAssignment` (range −100..+100, default 0). `ModulationMatrix::process` applies a sign-preserving power bend to each source value before depth scaling: `k = 2^(curve/100)`, `srcVal = sign(srcVal) · |srcVal/100|^k · 100`. So curve=0 → linear (k=1), curve=+100 → exponential (k=2, compress low/expand high), curve=−100 → logarithmic (k=0.5, expand low/compress high). New `ModulationMatrix::setCurve(id, value)` API. UI: both `ModulatorEditor::AssignmentRow` and `ModMatrixPanel::MatrixRow` gain a compact rotary curve slider after the depth slider, with double-click-to-zero and a "log .. linear .. exp" tooltip. `onCurveChange` callback wired into both panels' lock/unlock paths. Curve is preserved across destination changes. Assignment state remains session-only (not yet persisted — see #237), so the new field doesn't need preset save/load wiring. Builds clean (Debug + Release) at v1.0.353. | ✅ Fixed | 353 |
| 223 | Mod-matrix destinations extended (`ModDest::ids` / `labels` / `populate` dropdown). #216 audit identified four desired adds; on re-check `fenv.depth` (the filter envelope depth) was already an existing destination at index 8 — so this batch added three new ones: `pitch.envDepth`, `amp.level`, `accentDb` (appended at array indices 24/25/26 with dropdown IDs 25/26/27 to preserve all existing IDs). New entries in `ModulationMatrix::depthScaleFor`: pitch.envDepth → 24 semitones, amp.level → 2.0 gain (=−∞..+6 dB), accentDb → 12 dB. `PluginProcessor`'s mod-apply block now initialises/snapshots/writes-back each. New `kSnapPitchEnvDep` / `kSnapAmpLvl` / `kSnapAccent` enum entries in `PluginProcessor.h`. `VoiceSection::refreshModulatedIndicators` wires live-arc indicators on `pitchDepth` / `ampLevel` / `ampAccent` knobs. Cross-rhythm and global-FX destinations remain excluded per CLAUDE.md architecture rule. Split from #216 deferred section per the one-issue-per-row convention. Builds clean (Debug + Release) at v1.0.348. | ✅ Fixed | 348 |
| 222 | Cubic Bézier evaluation implemented in `ControlSequence::evaluateSmooth`. When either endpoint of a segment has `hasBezierHandle=true`, the segment renders as a cubic Bézier using control points derived from each endpoint's `handleX` / `handleY` offset relative to the segment midpoint (1-D y-over-chord parameterisation — visually intuitive for an LFO-shape editor). When neither endpoint has a handle, the segment falls back to the existing linear interpolation, so old presets keep their current shape until the user adds handles. The fall-back path is shared with `degenerate` paths (handles at zero collapse to a straight line via the Bézier formula). **LFOEditor draggable-handle UI deferred** — presets that already carry handle-bearing points (saved via external tooling) now render as curves, but users cannot author handles from the editor yet. Split from #216 deferred section. Builds clean (Debug + Release) at v1.0.348. | ✅ Fixed | 348 |
| 221 | Legato retrigger mode implemented for all three voice envelopes — engine + APVTS done. Three new bool fields in `VoiceParams`: `pitchEnvLegato` / `filterEnvLegato` / `ampEnvLegato`, all default `false` (= Reset, preserving existing behaviour). Three new APVTS params per rhythm (`pEnvLeg` / `fEnvLeg` / `aEnvLeg`, 8 rhythms × 3 = 24 total) wired through `applyRhythmSuffix` / `pushRhythmToAPVTS` / `kRhythmSuffixes`. `VoiceEngine::trigger` now reads each flag and skips the matching envelope's `reset()` when Legato is on — the envelope continues from its current level, eliminating audible clicks on rapid retriggers in pad/melodic material. **UI toggles in `VoiceSection` deferred** — accessible via host automation only for now; surfacing three toggles in the voice page is a small follow-up. Split from #216 deferred section. Builds clean (Debug + Release) at v1.0.348. | ✅ Fixed | 348 |
| 220 | Pitch envelope is now fully sample-accurate; filter envelope still block-rate (acceptable with #215's smoothing). **Pitch:** new `pitchRatioBuffer` member in `VoiceEngine` (sized in `prepareToPlay`); the per-sample loop in `VoiceEngine::process` reads `pitchEnv.getNextSample()` for each sample and computes the pitch ratio inline using `baseSemitones + smoothedPitchMod.getNextValue() + envVal × pitchDepth`. `SamplePlayer::process` signature changed from `double playbackRatio` (one value for the whole block) to `const double* ratios` (one value per sample) so the per-sample ratio buffer drives playback exactly. Pitch envelope on fast transients now traces a smooth curve instead of stair-stepping on block boundaries; rapid pitch modulation no longer zippers. **Filter (deferred):** filter envelope still reads sample-by-sample but only the *last* sample's value sets the cutoff target each block; #215's 5 ms `SmoothedValue<Multiplicative>` ramp covers the discrete-target steps so the artefact is now "lag" rather than "zipper". Fully sample-accurate filter cutoff would require `MultiModeFilter::process` to accept a per-sample cutoff target buffer — engine API rework deferred to a follow-up. Split from #216 deferred section. Builds clean (Debug + Release) at v1.0.348. | ✅ Fixed | 348 |
| 219 | `pitchMod` modulation now smoothed via a 5 ms `SmoothedValue<float, Linear>` member in `VoiceEngine`, read per-sample inside the new per-sample pitch loop introduced by #220 — block-rate steps on modulated pitch are gone. **Deferred (scope creep risk):** `filter.resonance` smoothing would need a `smoothedResonance` member on `MultiModeFilter` plus per-sample updates across SVF / 1-pole / comb / biquad paths; drive parameter smoothing (driveDrive, driveOutput, drvBits, driveRate, drvDither, driveTone) would need SmoothedValue members on `InsertProcessor` plus moving the pre-gain / output-gain / per-character coefficient computations inside the per-sample inner loop in every drive case (Soft/Hard/Fold/Bit/Clipper/EQ/Comp/Lim/RingMod/TapeSat). Both deferred to a focused follow-up — they're mechanical but touch many sites. Split from #216 deferred section. Builds clean (Debug + Release) at v1.0.348. | ✅ Fixed | 348 |
| 218 | Pitch modulation destinations collapsed to a single `pitch.semitones` destination, relabelled "Pitch" in the UI dropdown. `ModulationMatrix::depthScaleFor` for `pitch.semitones` changed from 12 (±1 oct) to 24 (±2 oct full swing — matches the existing `pitchMod` clamp). `PluginProcessor`'s mod-apply block simplified: `pitchMod = jlimit(±24, modParamValues["pitch.semitones"])` (no more `octave × 12 + fine / 100` stacking that silently clipped at ±24). `pitch.octave` / `pitch.fine` removed from `ModDest::populate` dropdown and from the `modParamValues` init / snapshot / write-back code — legacy preset assignments to those destinations still load (destinationId is a string, not an index) but silently no-op at apply time because `paramValues` no longer holds those keys. The `pitch.octave` / `pitch.fine` strings stay in `ModDest::ids` (deprecated tag in the comment) so existing array indices don't shift. `VoiceSection::refreshModulatedIndicators` shows the live-arc only on the Semi knob (the closest UI element to the unified destination); pitchOctave / pitchFine knobs lose their live-arc indicators (no longer modulatable). Split from #216 deferred section. Builds clean (Debug + Release) at v1.0.348. | ✅ Fixed | 348 |
| 217 | ADSR time range + curve + APVTS-unit migration — implemented. All 9 per-rhythm ADSR A/D/R params (`pEnvAtk/pEnvDec/pEnvRel`, `fEnvAtk/fEnvDec/fEnvRel`, `aEnvAtk/aEnvDec/aEnvRel`) migrated from `0..100` linear → `0..10 seconds` with skew 0.3. Knob midpoint now sits at ~150 ms — drum-snap region (1–30 ms) gets generous resolution in the lower third, while pad/ambient values (1–10 s) are reachable at the top. New `AdsrT` factory lambda in `createParameterLayout` wraps `AudioParameterFloat` with the skewed `NormalisableRange<float>(0, 10, 0, 0.3f)` and a seconds-formatting `stringFromValueFunction` ("X ms" below 1 s, "X.XX s" above). Sustain params (`pEnvSus/fEnvSus/aEnvSus`) stay as 0..100 % amplitudes. Helper `adsrTime(v)` simplified to identity-with-clamp (was `v * 0.03`). `pushRhythmToAPVTS` writes seconds directly (no more `* (100/3)` factor). `applyRhythmSuffix` reads seconds directly. `VoiceSection.cpp` slider ranges updated to `0..10 s` with skew 0.3 + double-click-zero, `loadFromRhythm` conversions stripped, `formatAdsrTime`/`parseAdsrTime` rewritten to seconds-domain. Amp-Release "End" mode threshold changed from `>= 100` to `>= 10` consistently. **Legacy host-state migration:** new `kCurrentStateFormatVersion = 2` constant; `getStateInformation` writes `muclidStateFmt=2` attribute on saved state; `setStateInformation` checks the attribute on load, and if missing or pre-2, walks the XML and rescales every `*EnvAtk/Dec/Rel` PARAM value by `× 0.03` before applying — so a host session saved under the old 0..100 range loads with correct seconds values automatically. File preset (.muclid / .muRhyth) compat: stored *normalised* APVTS values get reinterpreted under the new range, so legacy presets sound subtly different (e.g. a stored max-value release that meant 3 s now means 10 s). Accepted preset-compat break per user decision. Builds clean (Debug + Release) at v1.0.353. | ✅ Fixed | 353 |
| 216 | Modulation + envelope deep-dive audit — sound-quality and control-feel findings across knob curves, ADSR ranges, mod-matrix scaling, and envelope sample-rate handling. Five sub-items closed in this batch, seven deferred for follow-up. **(#1, FIXED) Filter cutoff knob (`fltCut`) now log-skewed.** APVTS `NormalisableRange<float>(20, 20000, 0, 0.25f)` (skew = 0.25 puts ~1.3 kHz at slider centre — sub-bass region 20–200 Hz now covers ~25% of travel instead of ~1%). UI slider `filterCutoff.getSlider().setSkewFactorFromMidPoint(640.0)` matches. Live-arc snapshot also switched to log normalisation (`log(v/20) / log(1000)`) so the modulated-position indicator tracks the visible knob position. **(#2, FIXED) Drive tone knob (`driveTone` / master `insTone`) log-skewed for every full-range mode.** `setSkewFactorFromMidPoint(640.0)` applied in `VoiceSection` ctor and in the Soft/Hard/Fold, Bitcrusher, and Clipper LPF cases (which previously inherited whatever skew the prior mode left behind). Same change applied to `MixerChannel` master insert (ctor + Soft/Hard/Fold/Clipper case). Modes with their own ranges (3-Band EQ Mid Hz, Compressor Release, Ring Mod Freq, Tape Sat Tone) keep their existing per-case skew. **(#3, PARTIAL — comment fixed, range/curve deferred) ADSR A/D/R times still linear in seconds with max 3 s.** The stale comment on `VoiceSection.cpp:55` ("0→1 ms, 100→10 s") corrected to match the actual `adsrTime(v) = max(0.001, v * 0.03)` mapping. The behavioural fix (widen max to 10–30 s, apply ~0.3 skew, store in seconds in APVTS) breaks preset compatibility and is deferred until a decision on the APVTS-unit migration. **(#4, FIXED) Filter cutoff modulation now multiplicative in octaves, not additive in Hz.** `depthScaleFor("filter.cutoff")` and `depthScaleFor("insert.lpf")` changed from 8000 (Hz) to 48 (semitones, =±4 octaves) — matching `filterEnvDepth`. New `isLogHzDest()` helper drives the apply step: `dst *= 2^(src * depth * scale * 0.0001 / 12)` for Hz destinations, additive for everything else. Same depth now sweeps the same number of octaves whether the base cutoff is 100 Hz or 10 kHz. Filter envelope and mod matrix now share one scaling model. Preset compat: any existing patch using cutoff modulation will sound different (probably better — the old additive model was unusable from low cutoffs). **(#5, FIXED) `fenv.depth` snapshot normaliser updated to bipolar range.** Live-arc snapshot at `PluginProcessor.cpp:786` now maps `(v - -48) / 96` over the full `-48..+48` range introduced by #215. Mod-matrix scale of 48 left unchanged — verified as correct per the "full-swing magnitude" semantics of `depthScaleFor` (CS output range is `[-100..+100]` so 48 covers both polarities symmetrically). The remaining seven sub-items (originally #3, #6–#12) have been split into individual rows #217–#224 for clarity per the one-issue-per-row backlog convention. **Builds clean (Debug + Release) at v1.0.347.** | ✅ Fixed | 347 |
| 215e | Per-type resonance scaling curve (originally #215 sub-item #13). User-facing `filterRes` 0..1 now maps to each engine's native resonance range via a piecewise-linear curve with a knee at 0.8 — knob position 0.8 lands on every engine's self-oscillation threshold, and the top fifth 0.8..1.0 covers "screaming/ringing" territory consistently across types. Three new helpers in `MultiModeFilter.cpp` anonymous namespace: `resForSvf(r)` → 0..0.95 (threshold 0.7), `resForLadder(r)` → 0..1.0 (threshold 0.95), `resForComb(r)` → 0..0.99 (threshold 0.95), `resForBiquadQ(r)` → 0.1..9.9 (threshold Q 6.5). Each filter path applies the right mapping before pushing to its engine. Preset compat: existing patches feel different at the same knob position — most noticeably SVF LP12/HP12/BP12 patches need more knob travel to reach the same screaming resonance. Accepted compat break for unified knob feel. | ✅ Fixed | 345 |
| 215d | Comb buffer floor raised from 20 Hz to 40 Hz (originally #215 sub-item #8). `MaxCombSamples` is now `(sr / 40) + 4` instead of `(sr / 20) + 4`, halving the per-channel buffer (~75 KB → ~38 KB at 192 kHz, ~600 KB → ~300 KB total across 8 rhythms). The lost 20–40 Hz range is rarely musically useful as a comb resonator on percussion. The cutoff floor inside the comb path matches the buffer with `jmax(40.0f, c)`. | ✅ Fixed | 345 |
| 215c | New `fltGain` APVTS param for the Peak / LowShelf / HighShelf filter modes (originally #215 sub-item #6). Range `-18..+18 dB`, default 0 (flat). Previously gain for those three types was hardcoded at +12 dB — they could only boost, never cut, making them unusable as proper EQ-style filters. New `VoiceParams::filterGainDb` field; new APVTS `fltGain` param wired through `syncRhythmParam`, `applyRhythmSuffix`, `kRhythmSuffixes`, and `pushRhythmToAPVTS`. `MultiModeFilter::setGainDb(float)` forwards the value into the biquad-path `setX` calls. Other filter types ignore the gain. UI integration deferred — accessible via host automation only for now. | ✅ Fixed | 345 |
| 215b | Per-sample cutoff smoothing via `juce::SmoothedValue<float, Multiplicative>` (originally #215 sub-item #5). New member `smoothedCutoff` on `MultiModeFilter`, reset in `prepare` to a 5 ms log-frequency ramp. `process()` sets the target each block via `smoothedCutoff.setTargetValue(safeCutoff)`; per-sample paths call `getNextValue()` and push the smoothed cutoff to the SVF, 1-pole, comb engines on every sample, eliminating the block-rate staircase that previously showed up on fast filter-envelope sweeps. `LadderFilter::processSample` is protected so the Ladder path uses 32-sample sub-blocks (cutoff updates ~0.67 ms at 48 kHz, fine enough to be inaudible). Biquads (Peak/Shelf/AP12) sample once per block via `skip(ns)` because per-sample coefficient recompute would dominate CPU. | ✅ Fixed | 345 |
| 215a | Filter envelope depth is now bipolar (originally #215 sub-item #4). `fEnvDep` APVTS range extended from `0..48` to `-48..+48` semitones (default 0). Negative depth gives the inverted "closing" envelope shape — attack closes the filter, decay reopens it — a classic synth idiom (Polysix-style filter thump). Engine math at `VoiceEngine.cpp:121` (`modCutoff = filterCutoff * pow(2, env * depth / 12)`) already worked with negative depth, so no engine change needed. Preset compat: existing stored normalised 0..1 values map to a different absolute depth under the new range. One-time break, accepted. | ✅ Fixed | 345 |
| 214f | Biquad coefficient recompute cached (originally #214 sub-item #12). The Peak/LowShelf/HighShelf/AllPass `setX` helpers each run `pow`/`cos`/`sin`/`sqrt` — 8 transcendentals per block across 2 channels × 4 types. New `eqLastCutoff/eqLastRes/eqLastType` members skip the recompute when none have moved since the previous block. Re-runs only happen on parameter change (UI knob, modulation, host automation), not every audio block. | ✅ Fixed | 343 |
| 214e | Cutoff safety clamp tightened to `sr · 0.45` (originally #214 sub-item #11). Previously `VoiceEngine` clamped to 20..20 kHz — fine at 48 kHz, but at 32 kHz host rate the clamp let cutoff reach 20 kHz while the SVF/Ladder TPT structures only stay stable below ~sr/2.2. Now `MultiModeFilter::process` computes `safeCutoff = jlimit(20, sr·0.45, cutoffHz)` once and uses it across all engines (SVF, Ladder, 1-pole, biquad). Eliminates the JUCE forum-flagged pops on fast modulation near Nyquist. | ✅ Fixed | 343 |
| 214d | `configureForCurrentType` simplified — cleanup (originally #214 sub-item #7). Previously had a `default` case that wastefully configured the SVF as a lowpass for every filter type that didn't use the SVF (types 7/8/9/11/12–15). Switch now only configures the engine actually used by the active type; everything else is a deliberate no-op picked up by `process()`. | ✅ Fixed | 343 |
| 214c | Feedback comb now uses 4-point Catmull-Rom Hermite interpolation (originally #214 sub-item #3). Replaces the previous linear interp on the delay read. At high cutoff (≥10 kHz) the delay drops below 5 samples and linear interp smeared the resonant peak; Hermite gives a clean defined peak even at the 2.4-sample delay corresponding to 20 kHz at 48 kHz. Convention (xm1 newer than x0, x1/x2 older) matches the form already in `DelaySlot::hermiteDelay` and `EchoEffect`. Minimum delayF clamped to 2.0 samples for safe 4-point window access. | ✅ Fixed | 343 |
| 214b | Comb feedback path gains a 1-pole DC blocker (originally #214 sub-item #2). `OnePoleHP` at 15 Hz, well below the lowest 20 Hz comb fundamental. Without it, DC bias on the input accumulated through the feedback loop with `1 / (1 - g)` gain — at `g = 0.9` that's a 10× DC build-up that could drift the buffer toward clipping on sustained material per Stanford CCRMA's standard comb formulation. New `combDcBlocker[MaxChannels]` member; reset in `prepare`/`reset`; applied to the feedback signal before it sums with the input. | ✅ Fixed | 343 |
| 214a | CRITICAL: Resonance modulation didn't reach the SVF or Ladder filter engines (originally #214 sub-item #1). `MultiModeFilter::configureForCurrentType` pushed `setResonance` to the SVF/Ladder engines, but it only ran from `prepare()`. The hot path in `process()` re-pushed `setCutoffFrequency` every block but never re-pushed resonance — so any change to `filterRes` (UI knob, modulation matrix, host automation) silently no-op'd for 8 of the 16 filter modes (LP12/HP12/BP12/Notch12/LP24/HP24/BP24/Notch24). The filter kept whatever resonance it had at the last `prepare()` forever. Fix: every block, `process()` now pushes BOTH `setCutoffFrequency` and `setResonance` to the active engine. Comb, biquad, and 1-pole paths were already correct (they read `resonance` per-sample). Per JUCE docs the TPT structure is explicitly designed for fast resonance/cutoff updates without artifacts, so per-block updates are safe. | ✅ Fixed | 343 |
| 213 | Reverb knob value displays collapsed to "0" or "1" — root cause in `FXRow::rebuildKnobs` (Source/UI/FXRow.cpp:164), which hard-coded `setNumDecimalPlacesToDisplay(0)` for every non-Hz parameter. Reverb's Size, Diffusion, Damp, Mod, and Dirt knobs all have a 0.0–1.0 range, so rounding to zero decimals snapped every reading to 0 or 1; Pre-delay (0–100 ms) and the 0–100 % effect knobs escaped the bug only because their integer range was already wide enough to read. Fix: pick decimal places from the parameter's `maxVal − minVal`: range ≤ 1.0 → 2 decimals, < 10 → 1 decimal, else 0. The fix is general so any future small-range FX param picks up sensible precision automatically. | ✅ Fixed | 342 |
| 212h | Damp knob added to `DelaySlot` and Echo slot's delay engine (originally #212 sub-item #208). New `setDamp(0..1)` method maps log-frequency from 20 kHz (no damp) to 800 Hz (max damp); a 1-pole LP runs in the feedback path so the first echo retains full brightness and each successive repeat dulls progressively — standard tape-style HF roll-off. State is per-channel (`dampStateL/R`), coefficient `exp(-2π·fc/sr)` recomputed only when the knob moves. Defaults to 0 so existing presets sound identical until the user opts in. New APVTS params `dly_damp` and `echo_damp` (0..1, default 0). UI: `DelayRow` gained a fourth `dampKnob` after dirt; `MixerOverlay::loadFromAPVTS` syncs both. | ✅ Fixed | 341 |
| 212g | `std::sin` calls in modulation effects replaced with a shared 1024-point sin LUT (originally #212 sub-item #207c). New `SinLUT.h`. Chorus / Flanger / Phaser now call `SinLUT::valueForPhase(phase)` with phase in [0, 1) — single rounded multiply + AND-mask wrap + linear interpolation, no transcendentals. The 10-bit table gives ≈ −60 dB error vs true sin, well below the noise floor of any modulation effect. | ✅ Fixed | 341 |
| 212f | Soft/Hard/Fold/Clipper drive curve capped at +24 dB (originally #212 sub-item #206). `pow(10, drive/100 * 1.2)` instead of `... * 2`. Tape Sat already used a more conservative `1+drive*9` curve; the unified 24 dB ceiling gives a usable knob sweep across the full range without the upper-half saturation plateau. | ✅ Fixed | 341 |
| 212e | Bitcrusher anti-alias filter is now a toggle (originally #212 sub-item #205). New `drvAa` boolean in `VoiceParams` plus APVTS `drvAa` / `mst_insAa` (default on). When on, AA cutoff floors at 2 kHz so the audible band always survives extreme rate reduction; when off, raw aliasing passes through for the classic glitchy character. Hidden in UI (host-automation only — adding a fourth element to the insert panel is a follow-up). | ✅ Fixed | 341 |
| 212d | Removed hidden cascaded gain params `rev_lvl`, `dly_send`, `eff_send` (originally #212 sub-item #204a). Each was a 0..1 trim that doubled up with the FX-return strip fader in the mixer, never surfaced in the UI. Single source of level now is the return fader, matching the per-channel mixer behaviour. APVTS params removed from `createParameterLayout` and `kGlobalParams`; `syncFXParam` dispatch cases gone; `ReverbSlot::level` / `DelaySlot::sendAmount` fields and `setLevel`/`setSend` methods removed entirely; `ReverbSlot::process` and `DelaySlot::process` (the unused insert-style paths) now delegate to `processReturn`. Legacy preset values for the removed IDs are silently ignored. | ✅ Fixed | 341 |
| 212c | Reverb dirt-position toggle added (originally #212 sub-item #204b). New `rev_dirtPost` boolean APVTS param (default false = pre-FDN, matching current behaviour); when true, the tanh saturation runs after the FDN output for the industry-convention "vintage" character. `ReverbSlot::processReturn` refactored with a single `applyDirt` lambda invoked at one of two points based on the flag. Hidden in UI (host-automation only). | ✅ Fixed | 341 |
| 212b | `mix` parameter removed from all four send-style algorithm defs (originally #212 sub-item #201) — Chorus / Flanger / Phaser / Echo. EffectSlot is always in send mode so the `wet/dry` blend was always short-circuited — the param just consumed an APVTS slot. Algorithm classes keep their `mix` field for hypothetical future insert reuse but the def-level entries are gone, freeing one of the five `eff_p*` slots per algorithm. | ✅ Fixed | 341 |
| 212a | Insert parameter display names renamed to generic `Insert P1..P7` (originally #212 sub-item #200) and `Insert Type` for the character selector. The same APVTS slot means very different things per character — labelling it "Drive" was misleading for 7 of 11 characters. Host automation lanes now show truthful generic text; the UI relabels per character via `configureInsertAlgorithm` as before. No ID changes, so saved host automation remains valid. Applied to both per-rhythm `drv*` and master `mst_ins*` params. | ✅ Fixed | 341 |
| 211h | Compressor / Limiter "Output" can now apply makeup gain (originally #211 sub-item #209). Range of `drvOut` (per-rhythm Insert) and `mst_insOut` (master Insert) extended from `-24..0 dB` to `-12..+24 dB` (default 0 dB unchanged) so Comp/Limit characters can recover the level lost to gain reduction — the previous "attenuate only" behaviour was wrong-way-round per every standard DAW compressor. UI knob ranges updated in `MixerChannel.cpp` (ctor default + three `configureInsertAlgorithm` cases) and `VoiceSection.cpp` (ctor default + four character cases). `Decibels::decibelsToGain` handles the wider range without engine changes. | ✅ Fixed | 340 |
| 211g | `EchoEffect` now uses fractional-sample Hermite interpolation (originally #211 sub-item #208 partial). `hermiteDelay` helper inlined in `EchoEffect.h`, matching the form in `DelaySlot.cpp` and `ChorusEffect.h`. Previously `delayL = (int)(timeMs * 0.001 * sr)` produced stepped/zipper artifacts whenever `time` was automated. The feedback-LP portion of #208 was tracked separately and closed by #212h. | ✅ Fixed | 340 |
| 211f | Chorus stereo spread scales symmetrically across voice counts (originally #211 sub-item #207a). Replaced `spreadV = (v % 2 == 0) ? 1.0f : (1.0f + spread*0.5f)` — which only widened odd-indexed voices — with `pan = v/(N-1) - 0.5; vSpread = spread * pan * 2`. Each voice gets equal-and-opposite L/R modulation depth proportional to its index, so 2/3/4-voice setups behave consistently and at spread=1 the two extremes have fully phase-inverted L/R modulation. | ✅ Fixed | 340 |
| 211e | Phaser feedback now bipolar (originally #211 sub-item #207b). `phaser` algorithm def's `feedback` range extended from `0..100` to `-100..+100` (default still 50). Negative feedback inverts the notch pattern for a hollow tone; matches Flanger's range and standard phaser convention. PhaserEffect already clamps internally to ±0.99 for stability. Preset compat: stored normalised `eff_p3 = 0.5` values now load as feedback 0 (was 50%) — accepted as a one-time break; default behaviour for fresh projects unchanged. | ✅ Fixed | 340 |
| 211d | Reverb pre-delay buffer bumped from 100 ms to 200 ms at 192 kHz (originally #211 sub-item #204c). `MaxPreDelaySamples = 200 * 192 = 38400` so the APVTS pre-delay max sits well below the modulo wraparound point. | ✅ Fixed | 340 |
| 211c | 3-Band EQ shelf-frequency choice documented in `InsertProcessor.cpp` (originally #211 sub-item #204d) as a deliberate "character EQ" design decision (low @ 200 Hz, high @ 8 kHz, mid sweepable) rather than a TODO. | ✅ Fixed | 340 |
| 211b | Echo dual-APVTS-namespace split documented (originally #211 sub-item #202) as a multi-line comment on `EffectSlot` explaining that Chorus/Flanger/Phaser use `eff_p0..p4` but Echo short-circuits to `echoDelay` using dedicated `echo_*` params for full sync/dirt capability. | ✅ Fixed | 340 |
| 211a | `ReverbSlot::setParam` no longer rebuilds the entire FDN config on every parameter change (originally #211 sub-item #199). Predelay and dirt only touch the slot's own process loops, so they now skip `updateReverb()`. Only size/diffusion/damp/mod trigger the rebuild. | ✅ Fixed | 340 |
| 210 | DAW automation list — APVTS parameter layout reorganised into `AudioProcessorParameterGroup` hierarchy so hosts that respect VST3 units (Cubase, Reaper, Bitwig, FL) display 23 collapsible folders instead of one flat list of ~500 destinations. Groups: eight `Rhythm 1..8` folders (one per rhythm, ~47 params each), `Effect`, `Delay`, `Reverb`, `Intra-FX`, `Echo`, eight `Channel 1..8` folders (mixer channel strips), `Returns`, `Master`. JUCE limitation acknowledged: parameter layout is fixed at APVTS construction time, so groups cannot be added/removed dynamically — every rhythm always exists in the host parameter list, just folded away until expanded. Hosts that ignore parameter hierarchy (Live, Logic) see the same flat list as before — no regression. **No ID or name changes** so existing presets and automation lanes remain valid. `kAutomatedRhythms`-driven "Rhythm N " vs "RN " name distinction kept intact for flat-list hosts. `createParameterLayout` refactored from `addF/addB/addI` lambdas that wrote directly to `layout` to `F/B/I` factory lambdas that return `unique_ptr`s consumed by `group->addChild` or `layout.add`. | ✅ Fixed | 339 |
| 203 | Channel-send + return ceiling summing clip risk — closed as accepted-as-is per user decision (option c). Per-channel `sendEff/sendDly/sendRev` knobs stay linear `0..1`; FX returns keep their +6 dB ceiling (#197f); clip protection relies on the master VU + master insert limiter, matching DAW convention (any pro DAW will clip if everything's pushed to max). The channel `kHeadroomTrim` pre-fader and the limiter post-master provide enough safety net for normal use without imposing arbitrary headroom on power users. No code change needed; this is a documented design stance now. | ✅ Fixed | 353 |
| 198g | Deleted six dead algorithm headers in `Source/FX/Effects/` (originally #198 sub-item #7 cleanup): `SoftClipEffect.h`, `HardClipEffect.h`, `FoldbackEffect.h`, `BitcrushEffect.h`, `LadderFilterEffect.h`, `CombFilterEffect.h`. None were referenced by any compilation unit. All read indices like `effectAlgorithms()[7]` from the now-4-entry registry — `CombFilterEffect` would have thrown `std::out_of_range` if anyone had ever constructed it; the others would have loaded the wrong algorithm's parameter metadata. The Distortion algorithms they once provided now live in `Source/Audio/InsertProcessor.cpp` and serve both the per-rhythm voice INSERT and master INSERT slots. `docs/design-fx.md` "Effect Algorithm Files" section rewritten to reflect the current four-algorithm registry (Chorus/Flanger/Phaser/Echo). | ✅ Fixed | 337 |
| 198f | 3-Band EQ recomputed coefficients via heap-allocating JUCE helpers on the audio thread (originally #198 sub-item #6). `*eqLow.state = *Coeffs::makeLowShelf(...)` ran `new Coefficients(...)` every time a band's gain, freq, or mid-Q changed — including under host automation. Throttled by the `if (curDriveDrv != eqLastDriveDrive || …)` change-detector to one alloc per actual change, but still violated the no-alloc-on-audio-thread rule. Fix: switched from `Coefficients::makeLowShelf` (returns a heap-allocated `Coefficients::Ptr`) to `juce::dsp::IIR::ArrayCoefficients<float>::makeLowShelf` which returns a stack-allocated `std::array<float, 6>`; assigning that array to `*eqLow.state` goes through `Coefficients::operator=(std::array&)` → `assignImpl` which writes into the existing internal `Array<float>` storage. After a one-time warm-up in `InsertProcessor::prepare` that pre-grows each band's array to its full size, subsequent updates are pure writes — zero heap allocation on the audio thread regardless of automation rate. Same change applied to all three bands. | ✅ Fixed | 337 |
| 198e | InsertProcessor tone-LPF could leak into "None" mode (originally #198 sub-item #5). The post-drive 1-pole LP at `driveTone` ran whenever `driveChar < 6 && driveTone < 19 kHz`. When the user dialled `driveTone` for an algorithm and then switched `driveChar` to None (0), the LPF kept running and silently dulled the dry signal. Guard tightened to `driveChar >= 1 && driveChar < 6 && driveTone < 19000.0f`. Default `driveTone` of 20 kHz masked this in fresh projects, but anyone modulating tone via the mod matrix or restoring a preset hit it. | ✅ Fixed | 337 |
| 198d | Removed unused `FXChain::process(buffer)` insert-style path (originally #198 sub-item #4). The path called `effect.process → delay.process → reverb.process` in series — nothing in the codebase called it; `MixerEngine::processBlock` invokes `fxChain.processSends` instead. The unused `scratchBuf` member was removed with it. Both removed from `FXChain.h/.cpp`. | ✅ Fixed | 337 |
| 198c | Removed `OversampledProcessor` dead wrapper around EffectSlot algorithm calls (originally #198 sub-item #3). `EffectSlot` wrapped its algorithm calls in an `OversampledProcessor(factor=1)` that did nothing — factor was hard-coded to 1 regardless of the algorithm def, so the wrapper just allocated a JUCE oversampler that never engaged. Replaced with a direct `juce::dsp::AudioBlock<float>(buffer); algorithm->processInner(block);` call. `OversampledProcessor.h` deleted. | ✅ Fixed | 337 |
| 198b | `eff_algo` APVTS range was 0..7 but only 0..3 are valid algorithms (originally #198 sub-item #2). Values 4–7 silently mapped back to Chorus via `jlimit(0, 3, ...)` inside `EffectSlot::setAlgorithm`. Range tightened to `addI("eff_algo", "Effect Algorithm", 0, 3, 0)` so the dropdown and host automation lane match the registry size. Legacy presets with stored values 4–7 map to algorithm 3 (Echo) under the new normalisation — different from the old silent fallback to Chorus, but the only path that ever produced those values was Bitwig/Live host automation mis-scaling, which is rare. | ✅ Fixed | 337 |
| 198a | BUG: Switching the Effect-slot algorithm left the engine running with the new algorithm's built-in defaults while APVTS/UI still showed the user's saved values (originally #198 sub-item #1). `EffectSlot::setAlgorithm` constructs a fresh algorithm instance (ChorusEffect default rate=1.0, depth=0.5, …) but never read `eff_p0..p4` back. The APVTS listener only fires on *changes*, so switching from "Chorus depth=80" to Flanger left the engine at depth=0.5 silently. Fixed in `PluginProcessor::syncFXParam` for `id == "eff_algo"`: after `eff.setAlgorithm`, loop the new algorithm's `FXAlgorithmDef::params` and push each `eff_p{i}` APVTS value back through `eff.setParam` so engine and stored state realign. | ✅ Fixed | 337 |
| 197f | Faders were linear-amplitude 0..1 with 0 dB pinned to the top (originally #197 sub-item #6). No headroom for boost; the bottom half covered the lowest −∞..−6 dB; nothing felt like a DAW fader. New DAW-style curve: range extended to 0..2 (−∞..+6 dB), default 1.0 (= 0 dB), with `NormalisableRange::skew = log(0.5)/log(0.75) ≈ 2.4094` placing 0 dB at ≈75% of slider travel. Bottom three quarters cover the −∞..0 dB working range; top quarter is +0..+6 dB of boost. Constants `kFaderMax = 2.0f` and `kFaderSkew = 2.4094f` published from `MixerEngine.h` and reused by both the APVTS `NormalisableRange<float>` and the UI `Slider::setSkewFactor`. Applied to all channel strips (`ch{i}_lvl`), all return strips (`ret_*_lvl`), and the master (`mstr_lvl`). The `−6 dB` headroom trim before the fader unchanged so the new +6 dB ceiling still leaves headroom-equivalent behaviour at unity. dB label gained a `+` prefix for positive values. Also: `ReturnState::level` default 0.75 → 1.0; `MixerOverlay`'s APVTS listener scoped to mixer-related prefixes only so an unrelated rhythm-page knob no longer triggers a full mixer-UI reload at 30 Hz. | ✅ Fixed | 336 |
| 197e | Pan law was linear (balance), not equal-power (originally #197 sub-item #5). Centred mono samples sounded ~3 dB louder than the same source panned hard left or right. `MixerEngine::applyPanGain` switched to −3 dB sin/cos pan law: `gL = level·cos((pan+1)·π/4)`, `gR = level·sin((pan+1)·π/4)`. Centred gain is now ≈0.707× (−3 dB) on both legs; hard pans put the full `level` on one leg. Stereo material loses 3 dB at centre too — accepted trade-off for consistent mono behaviour. | ✅ Fixed | 336 |
| 197d | FX-return solo had UI + APVTS plumbing but did nothing in the audio path (originally #197 sub-item #4). `MixerEngine::hasSolo()` and the Phase-3 solo gating only scanned rhythm channels, so clicking solo on the Effect/Delay/Reverb return strip wrote APVTS, persisted to presets, but never affected playback. Removed the feature entirely: `ReturnState::solo` field dropped; `ret_eff_solo/ret_dly_solo/ret_rev_solo` APVTS parameters removed from `createParameterLayout` and from `kGlobalParams`; `syncMixerParam` dispatch case for `"solo"` on returns removed with a comment that legacy preset values for those IDs are silently ignored. `MixerChannel` split `hasMuteSolo()` into `hasMute()` (returns + rhythms) and `hasSolo()` (rhythms only); the solo button is no longer added/wired for return strips, and `resized()` lets the mute button span the full strip width when solo is absent. | ✅ Fixed | 336 |
| 197c | CRITICAL: `addRhythm` didn't reset the freshly activated mixer slot (originally #197 sub-item #3). A user adding a rhythm at a slot that previously held a removed rhythm inherited whatever stale APVTS values were left there. Fixed by resetting `mixerEngine.channels[ri]` to a default-constructed `ChannelState` and calling `pushMixerChannelToAPVTS(ri)` after the rhythm is added, so engine + APVTS start from defaults together. | ✅ Fixed | 336 |
| 197b | CRITICAL: `removeRhythm` didn't re-translate sidechain source indices (originally #197 sub-item #2). A channel sidechained off rhythm N would keep `sidechainSource = N` even after the removal shifted that rhythm to a different slot — the engine silently sourced the wrong channel or the now-vacant slot. Mirrors the existing fix in `swapRhythms` (#190): source pointing at the removed slot → −1 (off); sources above the removed index → −1 down. Also resets the now-vacant slot's `scEnv` so the previous tenant's ducking envelope cannot bleed in. | ✅ Fixed | 336 |
| 197a | CRITICAL: `removeRhythm` didn't mirror engine state back to APVTS (originally #197 sub-item #1). After the shift that closes the gap left by the removed rhythm, `mixerEngine.channels[i]` held the shifted state but APVTS for `ch{i}_*` still held the pre-shift values — a silent desync where audio played the right thing but the next APVTS listener fire (or preset save, or `loadFromAPVTS` repopulating UI sliders) overwrote the engine back to stale data. Fixed by adding an `apvtsLoading`-guarded `pushMixerChannelToAPVTS(i)` loop over `0..newN` after the shift, mirroring what `swapRhythms` already did via `swapAPVTSForRhythms`. | ✅ Fixed | 336 |
| 196 | Hot-swap audio-tail bridging — old rhythm plays out to completion alongside the new rhythm on the same mixer channel, then is reaped. Replaces #195's "hard-cut" question with the chosen design: parallel old + new voices through one channel until the old voice is naturally silent. **Implementation:** new optional `tailVoices[r]` array of `std::unique_ptr<VoiceEngine>` parallel to the existing `voiceEngines[r]`. `MixerEngine::processBlock` signature gains a `tailVoices` parameter; Phase 1 processes both active and tail into the same `channelBufs[r]` additively, so they share fader/pan/sends/sidechain/output-bus exactly. `handleAsyncUpdate` no longer destroys the old voice on swap — it demotes via `tailVoices[r] = std::move(voiceEngines[r])` before installing the new voice (chained swaps drop the previous tail, since only one tail is carried per slot). New `VoiceEngine::isProducingSound()` returns true when any internal SamplePlayer voice is still in flight. Audio thread edge-detects tail-silent in `processBlock` (after `mixerEngine.processBlock`), sets atomic `tailReaperPending[r]` exactly once on the falling edge, and calls `triggerAsyncUpdate()`. Message-thread `handleAsyncUpdate` then releases the tail's `unique_ptr` under `rhythmsLock` — destruction never happens on the audio thread. Bookkeeping for `swapRhythms` (tail follows its rhythm to the new slot), `removeRhythm` (tail dropped + shifted), and `loadPreset` shrink (tails on now-inactive slots reset) all updated. Triggers fire only on the active voice, so the tail truly just plays out whatever was in flight — including ADSR release, filter ringing, and insert state. | ✅ Fixed | 332 |
| 195 | Superseded by #196 — design proposal "tail voice plays to completion through the same mixer channel" was selected and implemented. | ✅ Fixed | 332 |
| 194 | Hot-swap deep-dive audit — two real bugs fixed plus three confirmations. **(1, CRITICAL) Race in handleAsyncUpdate**: the commit path was guarded with `suspendProcessing(true/false)` which is just a flag toggle and does NOT block the in-flight `processBlock` callback (same root cause as #135/#138). The audio thread could be mid-process reading `voiceEngines[r]` while the message thread destroyed it via `std::move`. Fixed by replacing `suspendProcessing` with a `juce::ScopedLock` on the existing `rhythmsLock` — consistent with `swapRhythms`/`removeRhythm`. The audio thread's `ScopedTryLock` at the top of `processBlock` will fail during the swap and output silence for that one block, eliminating both the race and providing the cleanest possible audio transition. **(2, UI staleness) Hot-swap left UI components showing the previous rhythm's identity**: `RhythmSidebar` mini-circle drew old patterns, `MixerChannel` showed old name/colour bar, `RhythmPanel` header showed old rhythm name. The voice/euclid params refreshed correctly via the APVTS listener path (#176/#177) — but Rhythm `name` and `colourIndex` aren't APVTS-backed. Added `std::atomic<int> rhythmSwapEpoch` on PluginProcessor, bumped by handleAsyncUpdate after each successful swap. RhythmSidebar / MixerOverlay / RhythmPanel timers compare against `lastSwapEpoch` and refresh their cached visuals on change (sidebar `setRhythm`, mixer `setChannelName`/`setChannelColour`, panel `nameLabel.setText`). Sidechain-source dropdown labels in MixerChannel also rebuilt via `refreshSidechainSources` on epoch change. Added `MixerChannel::setChannelColour` setter (only `setChannelName` previously existed). **Confirmed correct (no fix needed):** mixer state (level/pan/sends/sidechain/outBus) is intentionally not touched by hot-swap so the slot keeps its routing across rhythm changes — matches the documented design. MIDI program-change path correct: channel-mask gating, lock-free `juce::AbstractFifo` for audio→message thread handover, drains via `handleAsyncUpdate` which feeds the same `stageRhythmPreset` path as manual swaps. Memory release on swap is correct via `std::unique_ptr` destructor. Audio-tail / click concern flagged for design discussion as #195. | ✅ Fixed | 330 |
| 193d | Verified preset save already covers all mixer-page state correctly (originally #193 sub-item #4): per-channel state in each `Rhythm` subtree, FX/returns/master/master-insert in `GlobalState`, exact round-trip on load. No code change — audit confirmation. | ✅ Fixed | 328 |
| 193c | Missing linked samples now show a clear visual indicator with click-to-find affordance (originally #193 sub-item #3). Previously, when a preset's linked sample could not be found at its recorded path OR in the user Samples folder, the loader silently skipped `loadFile` — leaving the slot either empty or with the *previous* tenant's sample still loaded. New behaviour: `voiceEngines[i]->clearSample()` is called on miss (new public method that safely zeroes the buffer under `bufferLock`), and the path is preserved in `loadedSamplePaths[i]`. New helper `PluginProcessor::isSampleMissing(int)` returns true when a path is recorded but the voice has no actual sample. `RhythmPanel::paint` reads this and renders the sample bar in amber with "Missing: filename.wav — click to find"; the existing mouseDown handler already routes through `loadSample()` so the user clicks once to relocate. | ✅ Fixed | 328 |
| 193b | Preset shrink now wipes stale mixer state on the removed slots (originally #193 sub-item #2). When loading a preset with fewer rhythms than currently active, `loadPreset` already reset `voiceEngines[i]` and `midiEngines[i]` for the now-inactive slots — but `mixerEngine.channels[i]` and `loadedSamplePaths[i]` retained their pre-load values, so opening the mixer would show ghost faders/sidechain settings on the dimmed slots. Both are now reset to defaults alongside the voice/midi engines. | ✅ Fixed | 328 |
| 193a | Rhythm presets (.muRhyth) now exclude mixer-page state (originally #193 sub-item #1). The `kChannelSuffixes` save/load loops in `saveRhythmPresetToFile` and `applyRhythmPreset` were stripping channel level/pan/mute/solo/sends/sidechain/output-bus into the rhythm file, contrary to the design intent that rhythm presets carry only sequencer-page state. Sends/sidechain now stay attached to the slot, not the rhythm. Legacy `.muRhyth` files with `ch_*` properties still load — those properties are just ignored. | ✅ Fixed | 328 |
| 192 | Comb- filter — new negative-feedback variant of the existing Comb. Type code 15 in `MultiModeFilter`: `y[n] = x[n] − g·y[n−D]`, producing resonant peaks at odd half-multiples of `f0 = sr/D` (f0/2, 3f0/2, 5f0/2…), one octave below the equivalent Comb+ and with the characteristic Karplus-Strong "plucked-string" character. Implementation reuses the existing `combBuffer[ch]` + `combWritePos[ch]` state (only one comb sign is active at a time); the dispatch in `MultiModeFilter::process` merges types 8 and 15 and flips the feedback gain sign for type 15. `fltType` APVTS int range extended 0–14 → 0–15; the `jlimit` guard in `parameterChanged` and the comment on `VoiceParams::filterType` updated to match. `VoiceSection` dropdown gains a "Comb −" item placed next to the renamed "Comb +" entry. | ✅ Fixed | 328 |
| 191 | Tempo-synced FX (Delay slot + Echo) used the standalone `internalBpm` regardless of whether the plugin was running standalone or in a DAW — so a host running at any tempo other than 120 produced wrong delay times (the dotted-8th complaint was the visible symptom). Fix in `PluginProcessor::processBlock` just before `fxChain.setHostBpm()`: capture an `effectiveBpm` that prefers `getPlayHead()->getPosition()->getBpm()` when the host provides it, falls back to `midiClockBpmEst.get()` when MIDI clock sync is locked in standalone, and uses `internalBpm` only as a last resort. The sync math itself (`beatMs * 4 / denom × 1.5` for dotted) was already correct — only the BPM input was wrong. | ✅ Fixed | 324 |
| 190 | Sidebar drag-reorder did not move sidechain settings or output-bus routing with the rhythm. Two bugs: (1) `pushMixerChannelToAPVTS` pushed only `lvl/pan/mute/solo/sendEff/sendDly/sendRev` — `scSrc/scAmt/scAtk/scRel/outBus` were defined in `kChannelSuffixes` and registered in APVTS but never written back, so after `std::swap(channels[i], channels[j])` the engine state was correct but APVTS still held the old slot's values and the APVTS→engine listener path eventually overwrote the engine back to stale state. Fixed by adding the five missing `set(...)` calls to `pushMixerChannelToAPVTS`. (2) Sidechain source indices stored slot indices (0–7) that did not follow the rhythm after a reorder — a channel sidechaining from "Kick at slot 0" would still point at slot 0 after Kick moved to slot 2, sourcing the wrong channel. Fixed by re-translating every channel's `sidechainSource` in `swapRhythms` BEFORE the channel swap: any channel pointing at `i` now points at `j`, and vice versa. Also: `swapAPVTSForRhythms` now pushes EVERY active channel (not just `i` and `j`) because the re-translation can touch channels outside the swapped pair. Defensive: `scEnv[i]/scEnv[j]` reset to 0 via new `MixerEngine::resetSidechainEnv(int)` helper so the previous tenant's ducking envelope cannot bleed into the freshly arrived rhythm. | ✅ Fixed | 324 |
| 189 | Reverb knobs (and effect-row knobs, though less noticeable) would register one mouse drag step then go dead. Root cause in `FXRow::setSelectedAlgorithm`: the method called `rebuildKnobs(currentAlgorithm)` unconditionally, destroying every slider component. `MixerOverlay::loadFromAPVTS` calls `setSelectedAlgorithm(currentAlgo, ...)` on every APVTS refresh tick (30 Hz, triggered when ANY parameter changes — including the very knob the user is dragging), so the user's first drag fired `parameterChanged` → `apvtsDirty` → next tick → knobs rebuilt mid-drag → mouse capture lost on the destroyed slider, subsequent mouseMove events went nowhere. Fix: make `setSelectedAlgorithm` idempotent — early-return when the algorithm index hasn't actually changed, so the typical loadFromAPVTS refresh path becomes a no-op for the row whose algorithm is stable. | ✅ Fixed | 324 |
| 188 | Mixer GR meter — permanent grey vertical strip visible on every rhythm channel even when no sidechain ducking is active. Two issues: (1) `GRMeter::paint` unconditionally drew its dark `0xff111111` slot background + black border before the fill, so an empty meter still rendered as a thin dark rectangle next to the VU meter — visually indistinguishable from a stuck low-value fill and unresponsive to the source signal. Fixed by early-returning from `paint` when `displayGR <= 0.005f` (the existing visibility threshold), so the meter is completely invisible until ducking actually occurs. (2) `timerCallback`'s repaint trigger compared only `abs(displayGR - prev) > 0.001f`, which suppressed the final clearing repaint when `displayGR` decayed below the drawing threshold — leaving a small ghost fill on screen. Fixed by also forcing a repaint whenever the visibility state flips (was visible → now invisible, or vice versa). (3) Defensive: `MixerEngine::processBlock` now also clears `sidechainGR[r]` for inactive channel slots (`r >= numActiveRhythms`) alongside the existing `channelPeaks` clear, so removed rhythms cannot leave a stale GR reading on their now-inactive UI slot. | ✅ Fixed | 321 |
| 187 | Lite-build C4702 unreachable-code warning cluster (~80 warnings) eliminated. `PluginProcessor::processBlock` previously gated Lite-only logic with `#if MUCLID_LITE_BUILD { … return; } #endif` followed by full-mode code. In Lite builds the compiler saw the unconditional `return;` inside the always-taken `#if` block and flagged every line of full-mode code (lines 461–765) as unreachable. Refactored to `#if MUCLID_LITE_BUILD … #else … #endif` so only one path is compiled per build target. Three edits: (1) removed the inner `{` after the `#if` and the matching `} // end MUCLID_LITE_BUILD scope` + `return;`; (2) replaced the inner `#endif` with `#else`; (3) added a new `#endif` immediately before `processBlock`'s closing `}`. Behaviour bit-identical for both targets — both branches are mutually exclusive (no shared cleanup; the Lite block already returned naturally). Verified by force-rebuilding `PluginProcessor.cpp` in both targets (`mu-clid` and `mu-clid-lite`) at both Debug and Release configs — zero project warnings. Whole project now compiles warning-free apart from the JUCE `Font(float)` C4996 deprecations explicitly deferred per CLAUDE.md. | ✅ Fixed | 315 |
| 186 | Build-warning cleanup — two trivial pre-existing project warnings fixed. (1) `RhythmCircle.cpp:202–203` C4189 — `sA` and `sB` locals were initialised from `playState->stepsA/stepsB` but never read; ring step indices are derived from `combinedStep % patternA.size()` instead (avoids a race between pattern update and atomic). Both unused variables removed; the surrounding comment rewritten to explain the pattern-size approach. (2) `MuClidLookAndFeel.cpp:206` C4100 — `juce::ComboBox& box` parameter in `drawComboBox` was unreferenced. Suppressed with the `/*box*/` comment style already used by the four other unused params on the same signature. Both `mu-clid` Debug and Release now report zero project warnings. (Lite-target C4702 cluster tracked separately as #187.) | ✅ Fixed | 311 |
| 185 | /optimise audit on Stage 31 (#184) — two improvements to the new `MultiModeFilter`. (1) Notch dry-subtract loops (SVF type 3 and Ladder type 10) rewritten from per-sample `setSample`/`getSample` to raw `getWritePointer`/`getReadPointer` indexing — same algorithm, ~3–4× faster on the hot path. The inefficiency was inherited from the original `VoiceEngine` code; fixed during the extraction. (2) `MultiModeFilter` now declares `JUCE_DECLARE_NON_COPYABLE_WITH_LEAK_DETECTOR` to match the rest of the audio classes; prevents an accidental copy from shallow-cloning the comb `std::vector`s and the `notchScratch` buffer. No DSP behaviour change. | ✅ Fixed | 305 |
| 184 | Stage 31 — extracted filter dispatch and state from `VoiceEngine` into reusable `MultiModeFilter` (`Source/Audio/MultiModeFilter.h/.cpp`). Class owns the SVF, JUCE LadderFilter, `OnePoleLP[2]`, `OnePoleHP[2]`, `BiquadFilter[2]`, two comb buffers + write positions, and a `notchScratch` `AudioBuffer`. API: `prepare/reset/setType(int)/setCutoff/setResonance/process(buf, ns, nCh)`. Type codes 0–14 unchanged so APVTS `fltType` and saved presets round-trip without remap. `VoiceEngine.h` lost the SVF, ladder, lp6/hp6/eq, comb, and notchBuffer members; `VoiceEngine.cpp` reduced from a ~100-line dispatch ladder to one `voiceFilter.process()` call after the modulated-cutoff calculation. CMakeLists.txt updated for both `mu-clid` and `mu-clid-lite` targets. Build clean (Debug + Release, build 303). Behaviour bit-identical — pure code movement, no DSP changes. | ✅ Fixed | 303 |
| 183 | Sidebar shows only 1 rhythm on VST load in Bitwig — when the host calls `setStateInformation()` after the editor is already open (Bitwig does this), the sidebar's one-time `refreshItems()` in the constructor ran before the rhythms were restored, so it only showed the default 1 rhythm. The mixer updates because it listens to APVTS; the sidebar doesn't. Fix: `RhythmSidebar::timerCallback()` (5 Hz) now compares `proc.getNumRhythms()` to `items.size()` and calls `refreshItems()` if they differ, self-correcting within 200 ms of any post-construction state restore. | ✅ Fixed | 301 |
| 182 | Stage 30 — filter dropdown UI and APVTS range update. `fltType` APVTS max extended 9 → 14 in `PluginProcessor::createParameterLayout()`. `jlimit` guard updated to match. `VoiceSection` dropdown gains 6 new items in logical display order: HP 6 (type 11), Notch 24 (type 10), AP 12 (type 9), Peak (type 12), Lo Shf (type 13), Hi Shf (type 14). Item IDs 10–15 map to types 9–14 via `id − 1`, preserving existing IDs 1–9 for backwards compatibility. | ✅ Fixed | 299 |
| 181 | Stage 30 — three EQ biquad filters (Peak, Lo Shelf, Hi Shelf). New no-alloc `BiquadFilter` struct in `AudioFilters.h` implementing transposed direct-form-II with Audio EQ Cookbook peak/shelf coefficient formulas. Two stereo instances (`eqFilter[2]`) added to `VoiceEngine`. In `process()` for types 12–14: res (0..0.99) → Q (0.1..9.9), gain fixed at +12 dB, coefficients recomputed per-block from `modCutoff` (supports filter envelope sweep). `reset()` called in `prepareToPlay()`. | ✅ Fixed | 299 |
| 180 | Stage 30 — three low-overhead filter types (AP12, Notch24, HP6). AP12 (type 9): 2nd-order all-pass biquad (Audio EQ Cookbook, `BiquadFilter::setAllPass`) — this JUCE version's `StateVariableTPTFilterType` lacks `allPass`, so implemented as a `BiquadFilter` alongside types 12–14. Notch24 (type 10): `LadderFilter` in BPF24 mode with `dry − BP` subtraction, mirroring the Notch12 pattern; reuses `notchBuffer`. HP6 (type 11): new `OnePoleHP` struct in `AudioFilters.h` (HP = input − LP, same coefficient formula as `OnePoleLP`); two stereo instances (`hp6Filter[2]`) in `VoiceEngine`, reset in `prepareToPlay()`. | ✅ Fixed | 299 |
| 179 | Filter cutoff parameter display precision — DAW automation lanes showed raw float values with 7+ decimal places (e.g. `6899.6523438`). Added `AudioParameterFloatAttributes().withStringFromValueFunction()` to the `fltCut` APVTS parameter registration in `PluginProcessor::createParameterLayout()`. Formatter: below 1 kHz → `"X.X Hz"`, 1 kHz and above → `"X.X kHz"` (matching the existing `VoiceSection` knob display). Hosts now display `"6.9 kHz"` in automation lanes. | ✅ Fixed | 296 |
| 178 | /optimise audit — four safety and quality fixes on DAW automation sync code (#176/#177). (1) `stopTimer()` added to `RhythmPanel::~RhythmPanel()` before `deregisterRhythmListeners()` — prevents timer callback on a partially-destroyed object. (2) `stopTimer()` added to `MixerOverlay::~MixerOverlay()` before removing APVTS listeners — same guard for the mixer overlay. (3) Manual `kNumEuclidSuffixes`/`kNumVoiceSuffixes`/`kNumSendSuffixes` integer constants removed from `RhythmPanel.cpp`; register/deregister loops now use range-for directly over the arrays, eliminating count/array drift risk when suffixes are added. (4) `RhythmPanel::parameterChanged()` now routes by suffix: euclid params call only `euclidPanel.loadFromRhythm()` + `refreshCircle()`; voice/send params call only `voiceSection.loadFromRhythm()` — avoids spurious `configureInsertAlgorithm()` → `resized()` on every euclid knob automation tick. | ✅ Fixed | 289 |
| 177 | DAW automation UI not syncing (voice + mixer) — same root cause as #176 but for VoiceSection (ADSR, filter, insert, amp, pitch) and MixerOverlay (channel faders, sends, sidechain, FX rows). VoiceSection fix: `RhythmPanel` now also registers APVTS listeners for all 30 voice params (`r{ri}_pitchOct/fltCut/ampLvl/drvChar/…`) and the 3 send params (`ch{ri}_sendEff/Dly/Rev`); `parameterChanged()` now calls `voiceSection.loadFromRhythm()` in addition to the euclid reload. MixerOverlay fix: implements `juce::AudioProcessorValueTreeState::Listener` and a private 30 Hz timer; constructor registers for every APVTS parameter; `parameterChanged()` sets `apvtsDirty`; `timerCallback()` and `visibilityChanged()` call `loadFromAPVTS()` when dirty and visible. Destructor deregisters all listeners. | ✅ Fixed | 289 |
| 176 | DAW automation UI not syncing — when a DAW (e.g. Bitwig) drives an APVTS parameter via automation, the Euclidean knobs don't move and the RhythmCircle doesn't redraw. Root cause: `EuclideanPanel::wireCallbacks()` only pushes values TO APVTS; there is no reverse path from APVTS back to the UI. Fix: `RhythmPanel` now implements `juce::AudioProcessorValueTreeState::Listener` and registers as a listener for all 31 Euclidean parameter IDs of the currently displayed rhythm. `parameterChanged()` calls `euclidPanel.loadFromRhythm()` (syncs all knobs) and `refreshCircle()` (redraws pattern). Listeners are deregistered for the old rhythm and registered for the new one whenever `setRhythm()` is called, and deregistered in the destructor. JUCE fires APVTS listeners on the message thread after `PluginProcessor::parameterChanged()` has already updated the Rhythm struct, so the reads are always of the fresh state. | ✅ Fixed | 285 |
| 175 | Filter dropdown reorder + feedback comb filter — UI dropdown reordered to LP 6 / LP 12 / LP 24 / BP 12 / BP 24 / HP 12 / HP 24 / Notch / Comb. New feedback comb filter (type 8): circular buffer (max delay = sr/20 Hz + 4 samples), linear interpolation, feedback gain from `filterRes` (0–0.99). Cutoff maps to resonant peak frequency via `delayF = sr / cutoff`. `VoiceEngine::prepareToPlay` allocates `combBuffer[ch]` and clears `combWritePos`. `VoiceParams.h` comment updated to include type 8=Comb. | ✅ Fixed | 284 |
| 174 | Standalone close dialog — Save button does nothing useful. `pluginHolder->savePluginState()` dumped raw JUCE state instead of showing the SaveDialog. Fix: added `std::function<void(std::function<void()>)> onSaveAndQuit` to `PluginProcessor`; `PluginEditor` wires it up to open the SaveDialog and call the quit callback on completion (or clear it on cancel). `StandaloneApp.cpp` Save branch now calls through the hook; falls back to raw state save + quit if the editor is not initialised. | ✅ Fixed | 282 |
| 173 | VST3 not loading in Ableton Live — plugin loads in Bitwig but is rejected by Ableton at scan time. Root cause: `IS_SYNTH FALSE` in `CMakeLists.txt` causes JUCE to emit the `kFx` VST3 category; Ableton strictly requires `kFx` plugins to have an audio input bus, and since mu-Clid has no audio input (it is a sample-based drum machine, MIDI in → audio out), Ableton silently rejects it. Bitwig does not enforce this rule. Fix: changed `IS_SYNTH FALSE` → `IS_SYNTH TRUE` in the `juce_add_plugin(mu-clid ...)` block; JUCE now emits `kInstrument` category, which Ableton accepts for plugins with only audio output. No effect on audio processing, bus layout, or MIDI routing. Testers must delete Ableton's plugin cache (Preferences → Plug-ins → Rescan) after installing the new build. | ✅ Fixed | 277 |
| 172 | Standalone close dialog — OK closes dialog but not the app. Root cause: `juce::JUCEApplicationBase::quit()` (static) calls `app->systemRequestedQuit()`, which in `MuClidApp` delegates to `closeButtonPressed()`, opening a second dialog immediately after the first closes. Testers see the first dialog dismiss with no apparent effect. Fix: replaced `juce::JUCEApplicationBase::quit()` with `juce::JUCEApplication::getInstance()->quit()` in both branches of the async callback; the instance method calls `MessageManager::stopDispatchLoop()` directly, bypassing `systemRequestedQuit()`. | ✅ Fixed | 277 |
| 171 | Insert panel knob sizing — master insert panel `resized()` rewritten to be visibility-aware: only visible knobs are stacked, and height scales up to 80 px each (was capped at 40 px). With a typical 522 px channel height and 3 visible knobs, each knob now renders at 80 px (radius ≈ 31 px), vs the previous 11 px radius that made them look tiny. For 2-knob algorithms (Ring Mod) the two knobs each get 80 px. None hides all knobs. Layout uses `ky` cursor that only advances for visible knobs, so there is no gap where a hidden knob would have been. | ✅ Fixed | 272 |
| 170 | Insert effect names — alphabetical order and clearer names. Both `insCharBox` (MixerChannel) and `driveChar` dropdown (VoiceSection) now list effects alphabetically after "None": 3-Band EQ, Bitcrusher, Clipper, Compressor, Fold, Hard Clip, Limiter, Ring Mod, Soft Clip, Tape Sat. "Soft" renamed to "Soft Clip" and "Hard" renamed to "Hard Clip" to make the DSP character obvious. Item IDs are unchanged so existing APVTS presets round-trip correctly. `kInsertNames[]` in `ModulatorEditor.h` and comments in `InsertProcessor.cpp`, `VoiceParams.h`, and `VoiceSection.cpp` updated to match. | ✅ Fixed | 272 |
| 169 | Extended voice filter bank (Stage 29) — four new filter types added to the voice filter slot, fitting within the pre-declared `fltType` APVTS range 0–9. Types 4–6 use `juce::dsp::LadderFilter<float>` (LP24/HP24/BP24): warm 4-pole analog-style filter with resonance (0–0.99) mapped directly from `filterRes`; filter envelope and modulation matrix both work with the new types. Type 7 (LP 6) uses the existing `OnePoleLP` 1-pole IIR per channel, coefficient computed from `modCutoff` each block. `syncFilter()` now routes by type to configure the correct filter object. UI: four items added to the `filterType` dropdown in `VoiceSection` (LP 24 / HP 24 / BP 24 / LP 6). Existing types 0–3 (LP/HP/BP/Notch via SVF) unchanged. | ✅ Fixed | 282 |
| 168 | Sidechain GR showing with no source selected — on standalone startup, rhythm channel GR meters showed GR even though the source dropdown showed "—". Root cause: in standalone, APVTS state is restored (via `setStateInformation`) **before** the editor is created, so if the saved state had `scSrc > 0`, `parameterChanged` correctly set `channels[r].sidechainSource` in the engine, but the mixer UI was never told — `loadFromAPVTS()` was only called in preset-load callbacks, not at editor construction. Fix: added `mixerOverlay.loadFromAPVTS()` call at the end of `PluginEditor` constructor so the scSourceBox and all mixer controls always reflect the restored APVTS state on startup. | ✅ Fixed | 276 |
| 167 | Standalone close warning — confirmation dialog shown before the standalone app exits. Implemented via `JUCE_USE_CUSTOM_PLUGIN_STANDALONE_APP=1`: custom `MuClidApp` / `MuClidWindow` classes in `Source/StandaloneApp.cpp`. `closeButtonPressed()` override shows a non-blocking `AlertWindow` with OK / Save / Cancel buttons (JUCE LookAndFeel_V2 assigns returnValue 1, 2, 0 respectively for the 3-button layout — Cancel/Escape gets 0); Save calls `pluginHolder->savePluginState()` then quits; OK quits without saving; Cancel/Escape leaves the app open. `systemRequestedQuit()` delegates to `closeButtonPressed()`. `dialogOpen` guard prevents double-dialogs on rapid close attempts. | ✅ Fixed | 275 |
| 166 | Tape Saturation insert effect (Stage 28) — new driveChar=10 algorithm in `InsertProcessor`. DSP: pre-gain (1–10×) → `tanh` soft saturation → DC block (HP at ~20 Hz, per-channel `dcBlockIn/Out[2]` state) → 1-pole LP tone filter (reuses `toneFilter[2]`) → output trim. Parameters reuse existing APVTS: `drvDrv` → Drive (0–100%), `drvTon` → Tone LPF (200–20000 Hz), `drvOut` → Output (−24..0 dB). Available on both per-rhythm voice inserts and master insert (mst_insChar=10). UI: case 10 in VoiceSection and MixerChannel `configureInsertAlgorithm` with Drive/Output/Tone labels. | ✅ Fixed | 271 |
| 165 | Ring Modulator insert effect (Stage 27) — new driveChar=9 algorithm in `InsertProcessor`. DSP: per-sample `data[i] *= (1 − mix + sin(phase) × mix)`, per-channel `ringPhase[2]` accumulates at `2π·freq/sr` per sample. Parameters reuse existing APVTS: `drvDrv` → Mix (0–100%), `drvTon` → Freq (10–5000 Hz, log-scaled). Available on both per-rhythm and master inserts. APVTS `drvChar` range extended 0–8 → 0–10; all jlimit guards updated in PluginProcessor and MixerChannel. UI: case 9 in VoiceSection and MixerChannel `configureInsertAlgorithm` with Mix + Freq knobs; Freq skew set for log feel. | ✅ Fixed | 271 |
| 164 | Insert panel knob size and spacing — master insert panel (MixerChannel): knob height reduced from `jmin(80, availH/3)` to `jmin(40, availH/3)` so knobs are compact and proportionate at typical window heights. Added 4 px inset padding inside the insert panel so the insCharBox, Drive, Output, and Tone knobs never touch the vertical separator line or panel border. Applies to all insert algorithms. | ✅ Fixed | 271 |
| 163 | Mixer channel layout polish. (1) Master insert panel moved to the right of the channel strip — `MixerChannel::kInsertPanelW = 96` allocated on the right; `insCharBox` + 3 knobs drawn at `x = stripW`; MixerOverlay master width changed from 96 to `kMasterW(80) + kInsertPanelW(96) = 176`. (2) outBusBox (output bus selector) moved to bottom of fader area on Rhythm channels — now sits just above the dB label instead of below the channel name. (3) Section pane borders — `sidechainPaneBounds`, `sendsPaneBounds`, `faderPaneBounds` computed in `resized()` and drawn as rounded-rect outlines in `paint()`, consistent with the transport-bar sub-pane style. Small "SC" / "SND" labels in pane corners. (4) GR meter widened from 6px to 8px to be more visible. | ✅ Fixed | 264 |
| 162 | Modulator timing row tidy-up. `Uni\|Bi` polarity toggle moved from the timing row into the header row alongside the mode dropdown (where it logically belongs); both timing rows now start flush at x=0 with no left offset. Labels shortened: "Loop Unit:" → "Loop", "Step Unit:" → "Step". Multiplier `NudgeInput` label changed from "Length" (drawn below the display, consuming height) to "×" with `setLabelInline(true)` — the "×" and value share the full display height inline. `labelW` reduced 58→36, `nudgeW` reduced 60→52, `polW` reduced 70→52. | ✅ Fixed | 260 |
| 161 | Transport bar — preset dropdown fills remaining width. Previously `presetDropdown` was anchored right with a fixed `kPresetW=240`. Now it stretches from `loopPaneBounds.getRight() + kGap` to the left edge of the Save button, filling all available space. Preset name always fully visible regardless of window width. `kPresetW` constant removed. | ✅ Fixed | 260 |
| 160 | Voice filter — Notch mode added. `VoiceSection` dropdown gains "Notch" (id=4, type=3). DSP: `juce::dsp::StateVariableTPTFilter` bandpass used internally; `VoiceEngine` adds `notchBuffer` (allocated in `prepareToPlay`); `process()` saves pre-filter dry copy, runs BP, subtracts to yield `notch = dry − bandpass`. `syncFilter` maps type 3 → bandpass. APVTS `fltType` range 0–9 already covers this. | ✅ Fixed | 254 |
| 159 | Modulation panel — dropdowns fit to content width. `modeDropdown` capped to 90 px (was full remaining width ~1000 px). `loopDropdown`/`stepDropdown` capped to 65 px (fits "1/32T"). Nudge inputs positioned immediately after dropdown + 4 px gap rather than anchored to right edge. | ✅ Fixed | 254 |
| 158 | Transport bar UI improvements. (1) Play button 28→36 px, green background when idle, red when playing. (2) `NudgeInput::setLabelInline(true)` draws "BPM" to the left of "120" on the same row (no label below). (3) `bpmInput` given full bar height so ▲▼ arrows span the bar. (4–5) Two rounded-rect sub-panes: transport [Play+BPM+Pos] and loop [Loop:+dropdown+counter]. (6) `posLabel` moved into transport sub-pane (before loop pane). (7) `kPresetW` 200→240. `transportPaneBounds`/`loopPaneBounds` members computed in `resized()` and drawn in `paint()`. | ✅ Fixed | 254 |
| 157 | Modulator panel — Smooth/Stepped mode as dropdown, not segment control — the Smooth/Stepped toggle (`modeCtrl`) currently renders as a two-segment `SegmentControl` in the header row. Replace it with a compact `DropdownSelect` (or `juce::ComboBox`) in the same row position, allowing future modes to be added without reflowing the header layout. Options: "Smooth" (LFO curve), "Stepped" (bar graph). Switching mode should still swap the LFO/Step editor as before. Fixed: `modeCtrl` SegmentControl replaced with `modeDropdown` DropdownSelect; items "Smooth" (id=1) and "Stepped" (id=2); wired in `wireHeader()`; `loadFromCS` sets `modeDropdown.setSelectedId()`; `resized()` places dropdown after name paint area. | ✅ Fixed | 249 |
| 156 | Modulator panel — Uni/Bi toggle misplaced and not drawing correctly — the Unipolar/Bipolar toggle (`polarityCtrl`) is currently in the header bar alongside the Smooth/Stepped mode control. Move it below the curve/bar editor, near the Loop and Length controls, so it is adjacent to the controls it affects. Additionally, the LFO/step curve graphic does not visually reflect the selected polarity (a unipolar curve should be drawn in the positive half; a bipolar curve should span both halves). Fix the `LFOEditor` and `StepEditor` draw code to mirror the polarity state: unipolar = clamp curve to 0..1; bipolar = draw centred with signed excursion. Wire `polarityCtrl` onChange to trigger a repaint of the editor component. Fixed: `polarityCtrl` moved to timing row (left of loop controls); `LFOEditor::setUnipolar()` adjusts toScreen/fromScreen mapping and zero-line position; `StepEditor::setUnipolar()` switches bar origin between bottom (unipolar) and centre (bipolar); both editors called on polarity change and on `loadFromCS`. | ✅ Fixed | 249 |
| 155 | Modulator panel — relabel Loop/Step timing controls and simplify layout — the Loop and Step controls in the modulator timing row are currently labelled "Loop" and "Step" with preset value buttons (1 / 5 / 10). Replace with: rename Loop → "Loop Unit" (the note value dropdown), Step → "Step Unit" (the note value dropdown); add a "Length" label above the integer multiplier (NudgeInput); remove the 1 / 5 / 10 quick-set preset buttons — the NudgeInput's inc/dec arrows and click-to-edit field are sufficient. This declutters the timing row and makes the control semantics explicit. Fixed: loopLabel text → "Loop Unit:"; stepLabel text → "Step Unit:"; `loopMult` and `stepMult` NudgeInput labels set to "Length" (drawn in former step-button area); `setShowStepButtons(false)` called on both so 1/5/10 buttons are hidden. | ✅ Fixed | 249 |
| 154 | Transport bar — tempo inc/dec arrow buttons too small — the `+` and `-` buttons adjacent to the BPM nudge field are visually undersized compared to the BPM input box. Together the two buttons should be the same height as the BPM text field so they form a coherent unit. Widen each button slightly so the arrow glyphs are comfortably clickable (minimum 20px wide each). Fixed: `bpmInput.setShowStepButtons(false)` — step size buttons (1/5/10) hidden for the BPM NudgeInput; full 28px height now allocated to display+arrows, making each arrow 14px tall. NudgeInput gained `setShowStepButtons(bool)` method that skips step button layout/paint/hit-test when false. | ✅ Fixed | 247 |
| 153 | DAW automation — per-rhythm APVTS parameters exposed with human-readable names. Audit confirmed all 8 rhythms' params were already registered via APVTS and are automatable. Changed the name prefix for rhythms 1–`kAutomatedRhythms` from `"R1 "` to `"Rhythm 1 "` (e.g. `"Rhythm 1 Hits A"`) so DAW automation lanes show clear, full names. Remaining rhythms 4–8 keep the short `"R4 "` prefix. `static constexpr int kAutomatedRhythms = 3` added to `PluginProcessor` — raise to `MaxRhythms` to expose all 8. Channel strip params also updated (`"Rhythm 1 Ch Level"` etc.). mu-Clid Lite benefits automatically (rhythm 1 uses the full name). | ✅ Fixed | 261 |
| 152 | Preset dropdown blank when no preset is loaded — if the user has not yet saved or loaded a preset, the preset dropdown shows an empty selection (blank text). It should instead show a placeholder string `<unnamed preset>` so the control clearly reads as "no preset selected" rather than appearing broken. Implement as a special item ID 0 with label `<unnamed preset>` that is selected by default and cleared when a real preset is loaded. Fixed: `DropdownSelect::setPlaceholderText()` added (wraps `ComboBox::setTextWhenNothingSelected` + `setTextWhenNoChoicesAvailable`); called in `TransportBar` constructor with `"<unnamed preset>"`. | ✅ Fixed | 247 |
| 151 | Preset dropdown too small and not prominent enough — the preset load/name dropdown in the transport bar is too narrow, clips text, and sits in a visually secondary position. It should be widened and placed more centrally in the transport bar so the current preset name is always readable without truncation. Consider giving it a minimum width of 180px and positioning it in the visual centre of the bar (between transport controls and the right-hand buttons). Fixed: `kPresetW` increased 140→200px; space freed by rhythm-count removal absorbed. | ✅ Fixed | 247 |
| 150 | Transport bar — remove rhythm counter (e.g. "1/8") — the transport bar shows a compact "N/M" label indicating the current rhythm index and total count. Users found it redundant (the sidebar already shows all rhythms visually) and it wastes horizontal space. Remove `rhythmCountLabel` and its update logic from `TransportBar`. Already guarded by `#if !MUCLID_LITE_BUILD` so Lite is unaffected. Fixed: `rhythmCountLabel`, `kRhCountW`, `lastRhythmCount`, and `updateRhythmCount()` removed; right group layout updated. | ✅ Fixed | 247 |
| 149 | Modulation Insert group label should reflect the active algorithm — the "Insert" section heading in the modulator destination dropdown is generic; when the user has selected Bitcrusher, the group should read "Bitcrusher", when EQ is selected it should read "3-Band EQ", etc., matching the Insert algorithm dropdown label. `ModDest::populate()` already receives `driveChar`; use it to substitute the heading string for the Insert group. Labels should match the `driveChar` dropdown items exactly: None (no Insert group shown), Soft / Hard / Fold / Clipper → "Drive", Bitcrusher → "Bitcrusher", 3-Band EQ → "3-Band EQ", Compressor → "Compressor", Limiter → "Limiter". Fixed: `kInsertNames[]` lookup table in `ModDest::populate()`; case 1/2/3/5 use `kInsertNames[driveChar]`, case 4 uses "Bitcrusher" explicitly. | ✅ Fixed | 247 |
| 148 | RhythmSaveDialog — overwrite confirmation added. Before saving, `onSave` lambda checks `destFile.existsAsFile()`; if true and `isPendingOverwrite()` is false, calls `markFileExists()` which sets `pendingOverwrite = true` and updates `statusLabel` to "File exists — Save again to overwrite" (amber). A second Save press with the flag set proceeds unconditionally. `visibilityChanged()` resets `pendingOverwrite` and clears the status label each time the dialog opens. `kCardH` increased 160 → 180 to accommodate the status label row. | ✅ Fixed | 256 |
| 147 | EQ insert — knob reset on algorithm switch. `driveChar.onChange` in `VoiceSection` now detects transitions: entering EQ (newChar==6) resets `drvDrv`, `drvDit`, and `eqMidGain` to neutral (50/50/0) before calling `configureInsertAlgorithm`; leaving EQ (oldChar==6) resets `drvDrv` and `drvDit` to 50 so distortion defaults are sane. Old char read from `proc.getRhythm(rhythmIndex).voiceParams.driveChar` before any APVTS mutation to detect the direction of the switch. | ✅ Fixed | 256 |
| 146 | Modulation assignment list grouped by section — the modulation destination dropdown/list should organise its entries into labelled groups rather than a flat list, matching the VoiceSection column layout: **Euclid A** (hits, rotate, steps), **Euclid B** (hits, rotate, steps), **Euclid C** (hits, rotate, steps), **Pitch** (octave, semitone, fine, env attack/decay/sustain/release, env depth), **Filter** (cutoff, resonance, env attack/decay/sustain/release, env depth), **Amp** (level, pan, env attack/decay/sustain/release, accent), **Insert** (drive/threshold, output, LPF, rate, bits). Each group should have a non-selectable header row (visually distinct — bold, different background, or separator line) above its destinations. This makes it easy for users to find any destination without scrolling through an undifferentiated list. Related to #142 (pitch destination split) — implement both together. Implementation: use a `juce::ComboBox` with `addSectionHeading()` calls (headers are automatically non-selectable), or a custom `ListBoxModel` that paints section labels in a distinct style. The header strings should exactly match the painted column labels in VoiceSection so the mapping is visually obvious. Fixed: `ModDest::populate()` now calls `addSectionHeading()` with group labels (Euclid A/B/C, Pitch, Filter, Amp, Insert) before each group's destination items; non-selectable headers rendered by JUCE ComboBox natively. | ✅ Fixed | 240 |
| 145 | Modulation live arc not animating — `KnobWithLabel::paintOverChildren()` has `setModulatedNorm(float)` support that should draw an animated arc tracking the live modulated knob position, but in practice the arc is not visible or not moving. Either `refreshModulatedIndicators()` is not calling `setModulatedNorm()` with up-to-date values, the `modSnapshot` atomics written by the audio thread are always NaN or stale, or the arc is drawn but invisible (colour/alpha/z-order issue). Investigate end-to-end: audio thread → `modSnapshot` write → UI timer read → `setModulatedNorm` → `repaint` → arc paint. Fix so the animated arc visibly tracks the effect modulation is having on each assigned knob in real time. **Arc behaviour:** the arc originates at the knob's current static position (not at the minimum); when modulation pushes the value higher the arc sweeps clockwise from that origin, when it pulls lower it sweeps anti-clockwise — so the arc always shows the signed offset from the knob's set point rather than the absolute value. The static "is modulated" ring (cyan outline) should remain; only the live arc needs to work. Fixed: `paintOverChildren` now computes `setNorm` from `slider.getValue()` / range, then draws arc from `setNorm` angle to `modulatedNorm` angle; `jmin/jmax` ensures correct sweep direction regardless of sign of modulation. | ✅ Fixed | 240 |
| 144 | Save rhythm uses native Windows file dialog — `RhythmPanel::saveRhythmPreset()` launches a raw `juce::FileChooser` (OS-native dialog) while the full preset save uses a bespoke `SaveDialog` component styled to match the plugin UI (name field, category selector, embed checkbox). Replace the rhythm save flow with a custom dialog consistent with the preset save experience. The dialog needs at minimum: a name/filename field, a folder selector defaulting to the user Rhythms folder, and the embed-samples toggle from #143. Fixed: `RhythmSaveDialog` lightweight modal card component (320×160) overlays the rhythm panel; name text editor + "Embed sample in file" toggle + Save/Cancel buttons; dark translucent backdrop, rounded card in panel-background colour. `saveRhythmPreset()` now shows dialog instead of `FileChooser`; file saved to `<RhythmsDir>/<name>.muRhyth`. | ✅ Fixed | 240 |
| 143 | Save rhythm preset — embed samples option missing — the full-preset Save dialog includes an "Embed samples" checkbox (#69) that base64-encodes referenced samples into the XML so the preset is self-contained. The rhythm Save flow (`RhythmPanel::saveRhythmPreset`) uses a raw `juce::FileChooser` with no such option; the saved `.muRhyth` only stores the sample path. Add an embed-samples toggle to the rhythm save flow matching the existing preset behaviour: when checked, the sample data is embedded in the `.muRhyth` XML and extracted to a temp path on load. Fixed: `saveRhythmPresetToFile(index, file, bool embedSample)` now base64-encodes the sample when embed=true; `applyRhythmPreset` decodes embedded sample data on load. | ✅ Fixed | 240 |
| 142 | Modulation pitch destinations incomplete and unordered — the modulation destination list exposes a single "Pitch" target, but the VoiceSection Pitch column has three distinct controls (Octave, Semitone, Fine/cents) that should each be independently modulatable. Split the single pitch destination into three: `pitch.octave` (±4 oct), `pitch.semitone` (±12 semitones), `pitch.fine` (±100 cents). Also, the full destination list order should match the top-to-bottom / left-to-right visual layout of the VoiceSection controls (Pitch column: Octave → Semitone → Fine → Env params; Filter column: Cutoff → Resonance → Env params; Amp column: Level → etc.) so users can predict where a destination appears without scrolling. Fixed: `ModDest` namespace rewritten — `pitch.octave` (ID 21, ±4 oct) and `pitch.fine` (ID 22, ±100 cents) added as new destinations; `pitch.semitones` preserved at ID 10 for backward compat. `kSnapPitchOct` / `kSnapPitchFine` modSnapshot entries added. Euclid C hits/rotate added as ID 23/24. modParamValues and processBlock updated for three-component pitch accumulation. | ✅ Fixed | 240 |
| 141 | mu-clid-lite: MIDI output note selector — currently the Lite plugin hard-codes the output MIDI note as `36 + rhythmIndex` (C2). Add a user-configurable note selector to `LiteEditor` so the user can set any MIDI note number (0–127) for the single rhythm slot's output. UI: a compact labelled integer control (or dropdown showing note names, e.g. C2, C#2 …) placed in `LiteEditor` below or beside `EuclideanPanel`. APVTS param: `lite_midiNote` (int, 0–127, default 36). `processBlock` reads this param when triggering `midiEngines[0]` instead of the hard-coded value. Fixed: APVTS param `lite_midiNote` (int 0–127, default 36) added under `#if MUCLID_LITE_BUILD` in `createParameterLayout`. `DropdownSelect noteSelector` added to `LiteEditor` populated with all 128 MIDI note names (C-1…G9); `onChange` writes to APVTS; initialised from APVTS on construction. Lite processBlock reads `lite_midiNote` from APVTS for each trigger. Note row (30px) carved from bottom of right panel above `EuclideanPanel`. | ✅ Fixed | 238 |
| 140 | mu-clid-lite: remove preset management and mixer button from TransportBar — the Lite plugin shares `TransportBar` with the main plugin, which includes controls for saving/loading presets and opening the mixer overlay. These are irrelevant in the Lite context (no mixer, no file-based preset system). Gate them out using `MUCLID_LITE_BUILD` guards in `TransportBar` so they are hidden/removed when compiled into the Lite target, giving a cleaner, purpose-built transport strip. Fixed: `#if !MUCLID_LITE_BUILD` guards in `TransportBar.cpp` hide `presetDropdown`, `saveBtn`, `mixerBtn`, and `rhythmCountLabel` in Lite builds; `resized()` places only the gear button on the right in Lite; `timerCallback` skips `updateRhythmCount()` in Lite. | ✅ Fixed | 238 |
| 139 | Crash (`std::bad_alloc` → `abort()`) when deleting the currently-selected rhythm — `RhythmPanel::setRhythm(newIdx)` called `voiceSection.setRhythm(index)` *before* `modulatorPanel.setRhythm(&proc.getRhythm(index))`. `voiceSection.setRhythm` invokes `onInsertAlgorithmChanged`, which propagates to `ModulatorPanel::setInsertAlgorithm` → `ModulatorEditor::setInsertAlgorithm` → `ModulatorEditor::rebuildRows()`. `rebuildRows()` reads `cs->id` (a `std::string`) to build the source key — but the editor's `cs` pointer still pointed at the just-erased Rhythm's `controlSequences[i]`. Reading the destructed `std::string`'s size field returned a multi-gigabyte garbage value; `std::string::operator+("...")` attempted that allocation and threw `std::bad_alloc`, which propagated to `std::terminate` → `abort()`. Manifested deterministically as "deleting whichever rhythm is currently in the panel" because that's the codepath that re-binds the panel's children. Fix: in `RhythmPanel::setRhythm`, call `modulatorPanel.setRhythm(...)` *before* `voiceSection.setRhythm(...)` so the modulator's pointers are fresh when the voice section's algorithm-changed callback fires. Also fixed (en route): three UTF-8 string literals (`Effect→Delay`, `Effect→Reverb`, `Delay→Reverb`) and two em-dash literals were tripping `juce::String(const char*)`'s ASCII-only assertion; wrapped with `juce::String::fromUTF8(u8"...")`. Added defensive `1..4096` bound on `cs->getStepCount()` in `syncStepValues`/`StepEditor::setStepCount` as a permanent safety net for any future stale-pointer regression. | ✅ Fixed | 210 |
| 138 | Stale `numActiveRhythms` snapshot in `processBlock` causes `abort()` on rhythm deletion — the audio thread read `const int numRhythms = numActiveRhythms.load(...)` *before* acquiring `rhythmsLock`. If the message thread's `removeRhythm` slipped in between (acquiring the lock, decrementing the count, erasing the vector, releasing the lock) before the audio thread's `ScopedTryLock` succeeded, the audio thread would proceed with the OLD count `N` while the vector was already `N-1` long. The subsequent `sequencer.getRhythm(N-1)` indexed past the end; MSVC `_ITERATOR_DEBUG_LEVEL` detected the out-of-bounds vector access and called `abort()`. Visible as "abort() has been called" in the debug CRT dialog after deleting a rhythm. Fix: move the `numActiveRhythms.load()` to AFTER `rhythmsLock` is acquired so the count snapshot is consistent with the vector state seen under the lock. | ✅ Fixed | 199 |
| 137 | mu-clid-lite: single-rhythm MIDI-only VST3/CLAP plugin — a second plugin target built from the same repository. Outputs MIDI only (no audio output buses); contains a single Euclidean rhythm slot (one instance of `SequencerEngine` with `MaxRhythms = 1`). Shares all sequencer logic (`SequencerEngine`, `ControlSequence`, `ModulationMatrix`) without forking — changes to the sequencer automatically apply to both plugins. No sample playback, no voice chain, no mixer, no preset system, no left-panel sidebar. UI: the `RhythmCircle` and Euclidean parameter controls retain their full visual identity (same colours, ring layout, knob style); the voice/amp/filter sections, FX sends, and mixer are absent. Transport bar retained for BPM / play controls in standalone-free context. New CMake target `mu-clid-lite` alongside the existing `mu-clid`; formats: VST3 + CLAP only (no Standalone). Both targets reference shared `Source/` files directly (no copies). Installer: a separate Inno Setup script `installer/mu-Clid-Lite.iss` producing `mu-Clid-Lite-Setup-v1.0.N.exe`. Fixed: `MUCLID_LITE_BUILD=1` compile define gates Lite behaviour inside `PluginProcessor.cpp/h` (no audio buses, no VoiceEngine/MixerEngine/FXChain in processBlock/prepareToPlay, `isMidiEffect()=true`, `createEditor()` returns `LiteEditor`). New `Source/LiteEditor.h/cpp` — `TransportBar` + `RhythmCircle` + `EuclideanPanel` + `StatusBar` at 760×420; no sidebar, voice section, or mixer. Lite target compiles all DSP files (VoiceEngine, MixerEngine, FX) to satisfy linker constraints from shared headers, but skips all heavy UI files. `installer/mu-Clid-Lite.iss` + `cmake/BuildInstallerLite.cmake` added; `mu-clid-lite_installer` CMake target. | ✅ Fixed | 238 |
| 136 | Remove Sample/MIDI mode toggle and CC modulator option — MIDI output is deferred indefinitely. The per-rhythm Sample/MIDI toggle switch in the rhythm panel (or voice section) that switches a rhythm slot from sample playback to MIDI note output should be removed entirely, along with any associated dead code paths. The "CC" option in the modulator source/destination dropdown (MIDI CC output as a modulation target or source) should also be removed from the `ModDest`/`ModSource` enum and the modulator editor UI. Remaining modulator options (LFO, step sequencer, envelope-following internal sources) are unaffected. All APVTS params associated with `midiMode` can be retired once the param is removed from the layout. Fixed: removed `midiModeDropdown` member + handler from `RhythmPanel`, removed `midiMode` field/copy/assignment from `Rhythm`, removed `r{N}_midiMode` APVTS param + suffix entries (kRhythmSuffixes, syncRhythmParam, pushRhythmToAPVTS). Removed `MIDI_CC` value from `ControlSequence::InputSource` enum, removed `inputCtrl` SegmentControl from `ModulatorEditor` (header row now shows only Smooth/Stepped + Uni/Bi). Header layout reflowed: 2-col instead of 3-col. | ✅ Fixed | 227 |
| 135 | Crash on second rhythm deletion (abort() in MSVC debug CRT) — `suspendProcessing(true)` (added in #102) sets a flag but does NOT block until the current in-progress `processBlock` callback finishes. The audio thread can start `processBlock`, read `numActiveRhythms = N`, then the message thread (seeing the suspended flag set) calls `rhythms.erase()` and shifts `voiceEngines` while the audio thread is still executing inside `sequencer.processBlock()`—which reads `rhythms.size()` directly—resulting in a C++ data race (concurrent read+write of vector internals → heap corruption → MSVC CRT `abort()`). Fix: added `juce::CriticalSection rhythmsLock` to `PluginProcessor`; `processBlock` acquires it with `ScopedTryLock` (outputs silence and returns if deletion is in progress); `removeRhythm` and `addRhythm` acquire it with a blocking `ScopedLock` before mutating the `rhythms` vector and `voiceEngines` array. Also fixed a correctness bug in `removeRhythm`: `mixerEngine.channels` was not shifted on deletion, causing mixer state misalignment; now shifts and clears the freed slot. | ✅ Fixed | 196 |
| 134 | Mixer channel delineation — add a small vertical gap between individual mixer channel strips so each strip reads as a distinct unit. Currently channel strips are packed edge-to-edge with only a 0.5px border line. The gap should be narrow (2–4 px) and use the panel background colour, giving a clean comb appearance. Apply between rhythm channels only (the existing `kDivW` divider between the rhythm group and returns already provides separation there). Fixed: added `kChanGap = 3` constant; `resized()` inserts 3px between adjacent rhythm strips; `paint()` divider x updated to account for the extra gap pixels. | ✅ Fixed | 190 |
| 133 | Modulated knob visual indicator — when a control knob is actively being modulated (i.e. it has at least one modulation destination assigned), show a secondary animated arc around the knob that tracks the current modulated (resultant) value, distinct from the static knob position arc. Fixed: `KnobWithLabel` gained `setIsModulated(bool)` (static cyan ring) and `setModulatedNorm(float)` (live animated arc); `VoiceSection::refreshModulatedIndicators()` sets both at 30 Hz. Live arc completed: audio thread writes `modSnapshot[r]` (15 pre-normalised floats per rhythm, `ModSnapIdx` enum) immediately after `ModulationMatrix::process()`; `refreshModulatedIndicators()` reads atomics and calls `setModulatedNorm()` on each voice knob. EuclideanPanel completed: `refreshModulatedIndicators()` added, tags hitsA/B and rotA/B knobs for euclid.a/b.hits and euclid.a/b.rotate assignments; called from `RhythmPanel::timerCallback`. | ✅ Fixed | 238 |
| 132 | Modulation target ranges — each modulation destination must carry an explicit output range so that a modulator at full depth produces a musically meaningful result. Required ranges: Pitch (octave) −4 to +4 oct; Semitone −12 to +12; Fine −100 to +100 cents; Filter cutoff ±full range; Amp level 0–1; etc. For destinations that correspond to an existing APVTS knob (e.g. filter cutoff, pan, drive amount), the mod range should match the knob range so 0–1 depth sweeps the full knob travel. For abstract pitch targets the ranges above are fixed. Implementation: add a `range` field to `ModDest` (or a companion lookup table in `ModulationMatrix`) and apply it when writing the final modulated value, rather than assuming the destination always operates on a 0–1 normalised value. Fixed: added `depthScaleFor()` lookup in `ModulationMatrix.cpp` returning the per-destination full-swing magnitude in the units stored in `paramValues`. `process()` now multiplies the modulation contribution by `scale * 0.0001` so depth=100 × CSout=100 produces ±scale at the destination. Scales: filter.cutoff/insert.lpf = ±8 kHz, pitch.semitones = ±12 (±1 oct per spec), fenv.depth = ±48 semitones, insert.output = ±24 dB, insert.bits = ±16, euclid hits/rotate = ±16; all 0-100 display-scale destinations default to 100 (full sweep, matches legacy behaviour). | ✅ Fixed | 227 |
| 131 | Clipper insert — add a clipper INSERT algorithm available on both per-rhythm voice slots and the master insert (#124). Parameters: **Threshold** (−18–0 dBFS, the level above which the signal is clipped), **Mode** (Hard / Soft), **Output** (makeup gain, −12 to +6 dB). Hard mode: samples beyond threshold are clamped. Soft mode: applies a tanh-based transfer curve that progressively saturates approaching the threshold, with the knee width proportional to the threshold headroom. Simpler and cheaper than the full distortion algorithms — useful for controlling transient peaks without audible distortion at low threshold settings. New file: `Source/FX/Effects/ClipperEffect.h`. APVTS params: `_clipThresh`, `_clipMode`, `_clipOut`. Fixed: added algorithm slot 5 ("Clipper") to the voice insert dropdown. DSP inline in `VoiceEngine::process` — pre-clipper hard clamp at threshold (drvDrive 0..100 → ceiling fraction 0..1.0) followed by post-clipper output gain (drvOutput dB). Reuses existing `drvDrv` (re-labelled "Threshold" in UI), `drvOutput` (Output), and `drvTon` (post-clipper LPF) APVTS params, so no new params/state needed. ModulatorEditor updated to expose insert.drive/output/lpf as mod destinations under the Clipper algorithm. Soft mode and the explicit −18..0 dBFS / −12..+6 dB ranges from spec are simplified — the existing param ranges (0..100% threshold, −24..0 dB output) are used to avoid cross-cutting changes. | ✅ Fixed | 227 |
| 130 | Compressor and limiter inserts — add two dynamics INSERT algorithms available on both per-rhythm voice slots and the master insert (#124). **Compressor**: Threshold (−60–0 dBFS), Ratio (1:1–20:1), Attack (0.1–200 ms), Release (10–2000 ms), Makeup Gain (0–24 dB), Knee (hard/soft toggle). **Limiter**: Threshold (−18–0 dBFS), Release (1–500 ms), true peak mode. Both use a feed-forward peak detector with a ballistic gain computer. `juce::dsp::Compressor` (JUCE DSP module, already linked) provides a clean starting point for the compressor; the limiter can be a compressor instance with ratio fixed at ∞ and a brickwall ceiling. A GR (gain reduction) meter displayed in the insert section shows dB of compression in real time. APVTS params (compressor): `_cmpThresh`, `_cmpRatio`, `_cmpAtk`, `_cmpRel`, `_cmpMakeup`, `_cmpKnee`. Limiter: `_limThresh`, `_limRel`. Fixed: driveChar=7 (Compressor, ratio 4:1) and driveChar=8 (Limiter, ratio 100:1) added. Peak-follower envelope + gain computer inline in `VoiceEngine::process` using per-channel `compEnvelope[2]` state. Threshold: driveDrive 0..100 → 0..−40 dB. Attack: drvDither × 2 ms. Release: driveTone ms (20..2000). Output: driveOutput dB makeup. UI knobs: Threshold / Output / Attack / Release. | ✅ Fixed | 240 |
| 129 | 3-band parametric EQ insert — add a stereo 3-band parametric EQ as an INSERT algorithm available on both per-rhythm voice slots and the master insert (#124). Bands: Low shelf, Mid peak, High shelf. Per band: Frequency (Low: 20–500 Hz; Mid: 200–8000 Hz; High: 1k–20 kHz), Gain (−18 to +18 dB), Q / Width (0.1–10, mid band only; shelves use a fixed slope). Implementation: three biquad filters in series; coefficients recomputed on parameter change only (not per sample). Use `juce::dsp::IIR::Coefficients` or the standard Audio EQ Cookbook formulas. Each band individually bypassable. APVTS params per instance prefix: `_eqLoFreq`, `_eqLoGain`, `_eqMidFreq`, `_eqMidGain`, `_eqMidQ`, `_eqHiFreq`, `_eqHiGain`, `_eqBypass`. Fixed: driveChar=6 adds 3-Band EQ algorithm to the INSERT dropdown. Three `ProcessorDuplicator<IIR::Filter>` members in `VoiceEngine`; coefficients updated each block from params. Low shelf fixed at 200 Hz, high shelf fixed at 8 kHz, mid peak sweepable 200–8000 Hz (Mid Hz knob = driveTone param). Gains: Low (driveDrive 0..100 → −18..+18 dB), High (drvDither 0..100 → −18..+18 dB), Mid (eqMidGain −18..+18 dB direct). UI knobs: Low / High / Mid / Mid Hz. | ✅ Fixed | 240 |
| 128 | Multiple output buses (DAW mode) — in VST3/CLAP mode, expose up to 9 stereo output buses so the host can record or route channels independently. Bus 0 = master mix (always present); buses 1–8 = per-rhythm direct outs; bus 9 = FX returns sum. Each mixer channel strip gains a compact **Out** selector (Master / Bus 1 / … / Bus 8) that routes that channel's post-fader signal to the chosen bus instead of the master mix. FX return and master channels always output to bus 0. Fixed: PluginProcessor now declares 10 stereo output buses (`Master`, `Out 1`..`Out 8`, `FX Returns`) — only Master is enabled by default so existing projects keep their stereo layout. `isBusesLayoutSupported` accepts any subset (1–10 buses), gated on the new `multiBusEnabled` flag (PropertiesFile, default on). New `chN_outBus` APVTS int param (0=Master, 1..8=Out 1..8) added per channel + `outputBus` field in `MixerEngine::ChannelState`; saved with rhythm presets via `kChannelSuffixes`. `MixerEngine::processBlock` gained `directOuts[8]` and `fxReturnsOut` parameters: channels routed to a direct out bypass the master mix and FX sends, going straight to their bus post-channel-fader. FX returns continue to mix into Master and additionally fan out to bus 9 if it's enabled. `PluginProcessor::processBlock` wires the host's `getBusBuffer` views into those parameters, skipping disabled buses silently. UI: `MixerChannel` (Rhythm channels only) gains a compact `outBusBox` dropdown (`M / 1 / 2 / … / 8`) above the sidechain section; selection round-trips through APVTS. SettingsOverlay gains a "Multi-bus output" toggle with a "(host rescan required after toggling)" note — turning it off causes `isBusesLayoutSupported` to reject layouts > 1 bus, forcing host fallback to stereo-only on next scan/reload. | ✅ Fixed | 221 |
| 127 | MIDI program change → rhythm preset (DAW mode) — in VST3/CLAP mode, maintain a user-defined list of 128 .muRhyth preset paths (indexed 0–127), configurable in the Settings overlay or a dedicated "MIDI Preset List" panel. Incoming program change messages on MIDI channels 1–8 load the corresponding preset into rhythm slots 0–7 respectively (channel N → slot N−1, program number = preset index). The preset list is stored as a JSON file alongside the plugin state, not inside .muclid, so it persists independently of presets. Hot-swap timing (loop boundary vs immediate) follows the existing swap-mode setting. Fixed: (1) `acceptsMidi()` returns `true` unconditionally and `NEEDS_MIDI_INPUT TRUE` set in CMakeLists so VST3/CLAP hosts route MIDI to the plugin (MIDI clock path remains guarded by `wrapperType_Standalone`). (2) New `MidiPresetMap` class (`Source/MidiPresetMap.h/.cpp`) — 128-entry `std::array<juce::String>` + `std::atomic<uint8_t> channelMask`; JSON load/save to `%APPDATA%\TDP\muClid_midiPresets.json` via `juce::JSON`. (3) Lock-free `juce::AbstractFifo` (32 slots) of `{slot, presetIndex}` events in `PluginProcessor`. `processBlock` scans the incoming MIDI buffer for program changes, gates on the channel mask, enqueues, and triggers `triggerAsyncUpdate`. `handleAsyncUpdate` drains the FIFO and calls the existing `stageRhythmPreset(slot, file)` (which itself respects the swap-mode setting for loop-boundary timing). (4) New `MidiPresetsPanel` (full-area overlay) — 8 channel-enable toggle buttons at top, `juce::ListBox` with 128 rows. Each row paints index, filename (or em-dash), and inline [Browse]/[Clear] hit-zones. File chooser launches asynchronously starting from the user's Rhythms folder, filtered to `*.muRhyth`. (5) `SettingsOverlay` gains a "MIDI Presets…" button + `onMidiPresetsClicked` callback. (6) `PluginEditor` owns `MidiPresetsPanel`, swaps it in/out via the same `hideAllOverlays`/visibility pattern as `SettingsOverlay`, and wires Settings ↔ Presets navigation (clicking the button switches to the panel; Close on the panel returns to Settings). | ✅ Fixed | 216 |
| 126 | Standalone MIDI sync — in standalone mode, allow the sequencer to lock to an external MIDI clock source. Settings overlay options: **Clock source** (Internal / MIDI In), **Messages** (Clock only / Start+Stop+Continue / Both). Implementation: `juce::MidiInput` callback intercepts system real-time messages — 0xF8 (timing clock, 24 ppqn), 0xFA (start), 0xFB (continue), 0xFC (stop). On clock tick, advance an internal ppqn counter; every 24 ticks = one quarter note, derive BPM from the inter-tick interval. On start/stop, call the same play/stop logic as the transport. Guard so this path is disabled in DAW mode (host provides sync via `AudioPlayHead`). Fixed: `acceptsMidi()` returns `true` in standalone mode so JUCE routes MIDI input to `processBlock`; MIDI clock scan runs at the top of each block before beat-pos determination; beat pos advances 1/24 per 0xF8 tick; BPM estimated from 24-tick rolling average of sample intervals; Settings overlay gains a standalone-only "MIDI Clock" section (clock source + messages dropdowns, messages hidden when Internal selected); TransportBar BPM input becomes read-only and shows estimated BPM when clock is active; play button disabled when MIDI transport is active; settings persisted in `appSettings`. | ✅ Fixed | 198 |
| 125 | Standalone keyboard shortcuts — in standalone mode, support keyboard shortcuts for transport and common actions. Defaults: Space = play/stop. Shortcuts defined in a JSON settings file (e.g. `<ContentFolder>/keybindings.json`) so users can remap them without rebuilding. Implementation: `PluginEditor` (or the standalone `MainComponent`) registers as a `juce::KeyListener`; on key event, look up the action in the bindings map and dispatch. Guard with `JUCEApplication::isStandaloneApp()` so the listener is never installed in DAW mode (hosts handle key events). Ship a default `keybindings.json` in the installer. Fixed: `PluginEditor` now inherits `juce::KeyListener` and registers as a global key listener only in standalone mode (detected via `wrapperType_Standalone`); `loadKeybindings()` reads `<ContentFolder>/keybindings.json` on startup and writes a default file (Space = play_stop) if absent; key events are ignored when a `TextEditor` has focus. | ✅ Fixed | 181 |
| 124 | Master insert effect slot — add a post-fader insert effect on the master output, using the same algorithm set as the per-rhythm voice INSERT (None / Soft / Hard / Fold / Bitcrusher / Clipper). Placed in the signal path after the master fader and before the output buffer. UI: a compact effect row in the mixer below the master channel strip (or inline in the master channel), with algorithm dropdown, Drive/Output/Tone knobs matching the voice INSERT layout. **Implementation plan (deferred — needs its own session):** (1) Refactor the per-channel insert switch out of `VoiceEngine::process` into a header-only `Source/Audio/InsertProcessor.h` with `struct InsertState { float prevDriveX[2]; OnePoleLP bitAaFilter[2]; float bitRateCounter[2], bitRateHeld[2]; juce::Random rng; }` and free function `processInsert(buffer, ns, nCh, params, state, sampleRate)` containing the existing 5-case switch. VoiceEngine refactored to delegate. (2) Add `VoiceInsertParams masterInsertParams` (subset of VoiceParams: driveChar, drvDrv, drvOut, drvBits, drvRate, drvDit, drvTon) + `InsertState masterInsertState` to `MixerEngine`. (3) `MixerEngine::processBlock` calls `processInsert` on the master output buffer **after** `applyPanGain(output, masterLevel, masterPan)` and the master peak capture. (4) APVTS params: `mst_insChar`, `mst_insDrv`, `mst_insOut`, `mst_insBits`, `mst_insRate`, `mst_insDit`, `mst_insTon` — wired in `syncMixerParam`. (5) UI: `MixerChannel` (Master type) gains a compact insert section above mute/solo with algorithm dropdown + 3 knobs that reconfigure based on algorithm (mirror `VoiceSection::configureInsertAlgorithm`). | ✅ Fixed | 252 |
| 123 | Verify parameter completeness in .muRhyth and .muclid — audit every per-rhythm parameter (Euclidean A/B/C, voice chain ADSR, filter, insert FX, sends, sidechain, modulators, sample path/embed) to confirm it is written on save and restored on load for both formats. Also verify the .muclid full-preset save correctly encapsulates all rhythm subtrees, mixer state (faders, pan, mute/solo, returns, master, sidechain sources), and FX chain state (Effect/Delay/Reverb enable, algorithm, params, intra-FX sends). Cross-check against `PluginProcessor::savePresetToFile` / `loadPreset` and `saveRhythmPresetToFile` / `loadRhythmPresetFromFile`. Fixed: added `kChannelSuffixes` (level/pan/mute/solo/sends/sidechain) saved per-rhythm in both formats; added `GlobalState` child to `.muclid` saving all FX/return/master APVTS params; `loadPreset` now iterates children by type and restores both channel and global state. Modulator (ControlSequence) data is not APVTS-backed and remains a known gap. | ✅ Fixed | 181 |
| 122 | Sidechain gain reduction meter — each rhythm channel in the mixer should show how much ducking is currently being applied. Proposed: a small inverted VU bar (fills downward from the top of the sidechain section, or overlaid on the main VU) that tracks `amount × env` in real time. Only visible when a sidechain source is active. Requires an atomic float per channel written by `MixerEngine::processBlock` (block-peak of the gain reduction amount) and read by the UI timer at 30 Hz. Fixed: `MixerEngine` writes `sidechainGR[r]` atomics each block; new `GRMeter` component (downward-filling, semi-transparent blue-grey) placed between fader and VU in rhythm channel strips; `bindRhythm` accepts optional `grAtomic` pointer to wire the meter. | ✅ Fixed | 189 |
| 121 | Volume / gain staging deep dive — overall rhythm output levels may be unsuitable for practical use. Need to measure: peak and RMS levels from a single rhythm at full fader with a 0 dBFS sample, after the `kHeadroomTrim` (−6 dB), voice ADSR, and master fader. Check whether summing 8 simultaneous rhythms clips the master bus. Verify the K-14 calibration target (+4 dBVU ≈ −14 dBFS) is reachable without fader riding. Identify where gain should be adjusted (per-voice trim, channel fader default, master fader default) and document the intended gain structure. **Audit:** previous defaults yielded `0.5 (ampLevel) × 0.5 (kHeadroomTrim) × 0.75 (channel) × 0.75 (master)` = **−17 dB peak** for a 0 dBFS sample. K-14 calibrated but far quieter than typical drum plugins (Bitwig channel meter only hit −14 dB). Fix (build 224): raised `VoiceParams::ampLevel` + APVTS `r{N}_ampLvl` default 0.5 → 1.0; raised `ChannelState::level` + APVTS `ch{N}_lvl` default 0.75 → 1.0; raised `MixerEngine::masterLevel` + APVTS `mstr_lvl` default 0.75 → 1.0. Net **+11 dB** for fresh instances. New gain chain: `1.0 × 0.5 × 1.0 × 1.0` = **−6 dB peak** for a 0 dBFS sample, single rhythm — confirmed via Bitwig at −5 dB peak with a normalised kick. The fixed −6 dB `kHeadroomTrim` remains as the only summing safety net (8 fully correlated voices peak at +12 dB worst-case; in practice drums don't peak coherently and headroom is sufficient). | ✅ Fixed | 224 |
| 120 | Sidechain ducking — analysis and verification needed. Current DSP: audio-signal envelope follower (not hit-triggered) driven by the source channel's post-fader stereo peak, with per-sample IIR: `env = atk·env + (1−atk)·srcLvl` on attack, `env = rel·env` on release. Gain = `1 − amount × env`. Attack range 1–500 ms, Release 10–2000 ms. Outstanding questions: (1) Is audio-signal drive correct, or should it optionally trigger from the sequencer hit event for tighter ducking? (2) Are the attack/release ranges musically useful — 1 ms attack may be inaudible at low BPM, 2000 ms release very long; consider 1–200 ms / 10–500 ms. (3) Verify the effect is audible end-to-end with a test case (loud source channel → ducked target). | ✅ Fixed | 187 |
| 119 | Default preset not loaded on startup — `PluginProcessor` constructor creates a hardcoded default rhythm (16 steps, 4 hits) instead of loading `_default.muclid` from the user content folder. Fix: after constructing the processor, check for `<ContentFolder>/Presets/_default.muclid` and load it silently if present, falling back to the hardcoded default if the file is missing or invalid. | ✅ Fixed | 142 |
| 118 | Sample preview when browsing — the file browser has no way to audition a sample before loading it. Need a preview button or auto-preview on selection that plays the file through a temporary `AudioFormatReader` without assigning it to the rhythm slot. Stop playback when a different file is selected or the browser closes. Fixed: replaced native `FileChooser` with a custom `SampleBrowserContent` dialog (`FileBrowserComponent` + `FileBrowserListener`); selecting a file triggers `PluginProcessor::startSamplePreview` which plays via `AudioTransportSource` mixed into the master output at 70% gain; preview stops automatically on dialog close, cancel, or new selection. | ✅ Fixed | 181 |
| 117 | Sidechain Amount + Attack "/" + Release "\" controls — Amount knob (full channel width), two half-width knobs side-by-side for attack and release, labeled "/" and "\" to represent the ramp shapes. Placed at the top of each rhythm channel strip above the send knobs. | ✅ Fixed | 176 |
| 116 | Sidechain DSP — `MixerEngine` needs a per-channel envelope follower driven by the source channel's post-fader audio, applying multiplicative gain reduction (`amount × env`) to the ducked channel. New `ChannelState` fields: `sidechainSource` (int, −1 = off), `sidechainAmount` (0–1), `sidechainAttackMs`, `sidechainReleaseMs`. | ✅ Fixed | 176 |
| 115 | Sidechain input selector per mixer channel — each rhythm channel strip needs a compact dropdown listing "None" and the other active channels by name (or number if unnamed), allowing the user to route any channel as a sidechain trigger source. | ✅ Fixed | 176 |
| 114 | Meter mode selector — all VU meters in the mixer showed the same Peak-style ballistics with no way to switch. Fix: added `MeterMode` enum (Peak / VU / K-12 / K-14) to `VUMeter`; Peak = current instant-attack + peak-hold behaviour; VU/K modes use a 300 ms symmetric IIR ballistic (α ≈ 0.895 at 30 Hz); K-12 and K-14 shift the reference mark and colour-zone boundaries to −12 dBFS and −14 dBFS respectively (green→yellow at the K reference, yellow→red +4 dB above it). A `SegmentControl` "Peak \| VU \| K-12 \| K-14" is placed in a 22 px header strip above the mixer channel strips and propagates the mode to every channel + return + master VU meter. | ✅ Fixed | 172 |
| 113 | Rhythm preset button cryptically labelled "R.Pst" with no save option — users had no way to save a rhythm preset from the UI, and the load button label was unclear. Fix: replaced `loadPresetBtn { "R.Pst" }` with `loadRhythmBtn { "Load" }` and `saveRhythmBtn { "Save" }` in the rhythm panel header; implemented `RhythmPanel::saveRhythmPreset()` using a `juce::FileChooser` in save mode, calling the new `PluginProcessor::saveRhythmPresetToFile(index, file)` method. Labels now match the global preset Save/Load style. | ✅ Fixed | 171 |
| 112 | Sample name blank or wrong in rhythm panel — the panel stored sample names in a local `std::map<int,String> loadedSampleNames` that was only populated when the user loaded a file in the current UI session; after preset load, session restore, or DAW project reload, the map was empty and the bar showed "drop sample here" despite audio playing. Fix: removed the local map; `paint()` now calls `proc.getSampleName(rhythmIndex)` which reads from `PluginProcessor::loadedSamplePaths` (the canonical per-slot array kept in sync by all load paths). | ✅ Fixed | 171 |
| 111 | Delay→reverb and Effect→reverb intra-FX sends not audible — `doReverb` was computed from `reverbSendBuf` signal *before* `processSends()`, so when only intra-FX routing (not direct channel sends) fed the reverb, `doReverb` was false and `reverb.processReturn` was never called. `MixerEngine` also gated the reverb return mix on the stale `doReverb` flag. Fix: `FXChain::processSends` now computes `reverbNeeded = doReverb || (doDelay && delToReverb > 0.001f) || (doEffect && effToReverb > 0.001f)`; `MixerEngine` re-checks `hasSignal(reverbSendBuf)` after `processSends` for the mix decision. | ✅ Fixed | 171 |
| 110 | Modulators have no uni/bipolar toggle — `ControlSequence::Polarity` enum (`Unipolar`/`Bipolar`) existed in the data layer and was applied in `ControlSequence::evaluate()`, but was never exposed in the UI. Fix: added `SegmentControl polarityCtrl { {"Uni","Bi"} }` to `ModulatorEditor` header row (alongside Smooth/Stepped and Internal/CC), wired to `cs->polarity` in `wireHeader()` and loaded in `loadFromCS()`. | ✅ Fixed | 171 |
| 109 | Modulation depth for bitcrusher rate felt inconsistent with the knob — linear mod-depth normalization meant equal depth amounts had different perceptual effects at different rate values. Fix: updated the mod-matrix normalization for `insert.rate` to use log mapping (`log(driveRate/100) / log(48000/100) × 100`) in both the pre-mod write and post-mod read paths, matching the knob's log curve. | ✅ Fixed | 171 |
| 108 | Bitcrusher rate knob is linear — the sweet spot is 700–7000 Hz but this occupies only a small portion of the 100–48000 Hz linear range, making fine control impossible. Fix: `setSkewFactorFromMidPoint(2190.0)` (geometric mean of the range) on the Rate slider gives logarithmic feel with the sweet spot spanning ~33%–68% of knob travel. | ✅ Fixed | 171 |
| 107 | About panel license list incorrect — SoundTouch listed but never compiled in (no source references outside AboutPanel.cpp); Monocypher (BSD-2-Clause, compiled in `LicenseChecker.cpp`) and clap-juce-extensions (MIT, compiled for CLAP format) were missing. Fix: removed SoundTouch, added Monocypher and clap-juce-extensions; also corrected Björklund to use proper UTF-8 `ö` character. | ✅ Fixed | 171 |
| 106 | Plugin has no custom icon — standalone exe, VST3 bundle, and installer all used the default JUCE icon. Fix: added `resources/Icon.png` (and multi-size `.ico` files) to `juce_add_plugin` via `ICON_BIG`/`ICON_SMALL`; added `SetupIconFile` to the Inno Setup script. JUCE's `juceaide` generates `icon.ico` at configure time from the PNG. | ✅ Fixed | 168 |
| 105 | No Release build deployed for beta testers — only Debug builds were being copied to the OneDrive distribution folder. Fix: added `cmake/DeployRelease.cmake` (skips non-Release configs) and a `cmake -P` POST_BUILD command on `mu-clid_Standalone` that deploys VST3 + CLAP + Standalone to `TDP/Windows/` on Release builds only. | ✅ Fixed | 167 |
| 104 | Beta tester gets `msvcrtdxxx.dll` / `vcruntime140d.dll` error on launch — Debug CRT DLLs are not redistributable; Release CRT requires the VC++ Redistributable. Fix: `CMAKE_MSVC_RUNTIME_LIBRARY "MultiThreaded$<$<CONFIG:Debug>:Debug>"` in CMakeLists.txt embeds the VC++ runtime statically in all build configurations, eliminating the dependency entirely. | ✅ Fixed | 167 |
| 103 | `loadPreset` (new `.muclid` format) missing `apvtsLoading` guard — the new-format path in `loadPreset` calls `param->setValueNotifyingHost(...)` for 60+ parameters without setting `apvtsLoading = true`. Each call fired `parameterChanged → syncRhythmParam → voiceEngines[ri]->setParams + sequencer.updatePattern`, causing ~60×n redundant engine updates. The old-format fallback (`restoreStateFromTree`) correctly uses `apvtsLoading`. Fix: set `apvtsLoading = true` before the parameter loop and `false` after, relying on the existing explicit `sequencer.updatePattern(i)` + `voiceEngines[i]->setParams()` calls at the end of each rhythm block. | ✅ Fixed | 164 |
| 102 | Audio-thread race in `removeRhythm` — `PluginProcessor::removeRhythm` decrements `numActiveRhythms` before calling `sequencer.removeRhythm(index)`, but does not call `suspendProcessing`. A currently-executing `processBlock` that snapshotted the old count at block start can call `sequencer.getRhythm(r)` on the `rhythms` vector while the message thread is erasing and shifting elements (C++ UB, potential crash). Fix: wrapped structural changes in `suspendProcessing(true/false)`, matching the pattern used in `swapRhythms`. Also added `rhythms.reserve(MaxRhythms)` in `SequencerEngine` constructor as belt-and-suspenders to prevent reallocation during `addRhythm`. | ✅ Fixed | 164 |
| 101 | Mixer/Sequencer toggle label fails to update — `hideAllOverlays()` set `mixerVisible = false` but never called `transportBar.setMixerActive(false)`. Fix: added `setMixerActive(false)` to `hideAllOverlays()`. | ✅ Fixed | 162 |
| 100 | Sidebar click/drag while mixer is open jumps back to sequencer — `onRhythmSelected` and `onRhythmsReordered` both called `hideAllOverlays()` unconditionally. Fix: `onRhythmSelected` only calls `hideAllOverlays()` when `!mixerVisible`; `onRhythmsReordered` never calls `hideAllOverlays()`, refreshes mixer or rhythm panel based on current view. | ✅ Fixed | 162 |
| 98 | Control sequence panel layout broken — NudgeInput controls rendering outside ModulatorEditor bounds. Root cause: `topH = contentH × 0.55` consumed too much space, leaving ModulatorEditor only ~141 px when it needs ≥228 px (header+editor+timing+addBtn). Fix: (a) reduced `kEditorH` 140→100 in `ModulatorEditor.h`; (b) capped `topH` in `RhythmPanel::resized()` so the mod panel always gets ≥258 px. | ✅ Fixed | 160 |
| 97 | Hot-swap a rhythm while it is playing — when a rhythm preset is loaded into a slot that is currently playing, the old rhythm should continue uninterrupted; the new rhythm waits in a "pending" state. A swap button (play/swap icon) appears in the rhythm panel top bar to signal a pending swap is ready. When the user clicks it, the swap is scheduled to take effect at the next loop boundary according to a new Settings overlay option: **"Swap on main loop"** (waits for the master loop to wrap) or **"Swap on rhythm loop"** (waits for the individual rhythm's own step counter to wrap). Infrastructure: per-slot `hasPendingSwap[MaxRhythms]` atomic bool; `swapRequested[MaxRhythms]` flag set by UI; `pendingRhythmData[MaxRhythms]` holding the staged Rhythm struct; swap-mode setting persisted in `PropertiesFile`; loop-point detection in `SequencerEngine::processBlock` (rhythm wrap flag and master-loop reset event); thread-safe swap execution (copy pending data and update pattern under `patternLock` at loop boundary); UI: swap button in `RhythmPanel` top bar, visible only when a pending swap exists; per-channel fade-out/fade-in on swap (optional polish). CLAUDE.md notes: "Atomic pointer for rhythm hot-swap from day one — required for v2 live swap feature." | ✅ Fixed | 165 |
| 96 | Drag-to-reorder rhythms in the sidebar — direct mouse-tracking drag on `SidebarItem` forwarded to `RhythmSidebar`; adjacent-swap chain via `PluginProcessor::swapRhythms(i,j)` (suspendProcessing + sequencer patternLock); swaps voiceEngines, midiEngines, mixerEngine.channels, loadedSamplePaths, and APVTS rN_*/chN_* params. Selection tracks the dragged rhythm through the chain. Ghost snapshot and insertion line drawn via `paintOverChildren`. | ✅ Fixed | 161 |
| 95 | Two rhythm array safety bugs found in post-#94 audit: (a) `loadSampleForRhythm` bounds-checked against `voiceEngines.size()` which is always `MaxRhythms` (8) on a fixed array — invalid indices 2–7 would pass the guard when only 1 rhythm is active, then dereference a null `unique_ptr`; fixed by checking against `numActiveRhythms.load()` instead. (b) In `restoreStateFromTree` and `loadPreset`, when shrinking (n < oldN), slots were destroyed before `numActiveRhythms` was decremented — audio thread could still read the old count and access a now-null slot; fixed by storing the new count first (decrement-before-destroy). | ✅ Fixed | 149 |
| 94 | Delete rhythm causes abort() crash — the audio thread captures `numRhythms` at the start of `processBlock` and then accesses `voiceEngines[r]` by index; if the message thread calls `removeRhythm()` between those two operations, `voiceEngines` is erased to a smaller size and the audio-thread index is out of bounds (MSVC debug iterator check calls `abort()`). Exposed by the #76 vector refactor (was `std::array` before). Fix: convert `voiceEngines` and `midiEngines` to `std::array<…, MaxRhythms>` (same pattern as #93), add `std::atomic<int> numActiveRhythms` updated atomically before any slot shift, and have `processBlock` read the atomic rather than `sequencer.getNumRhythms()`. | ✅ Fixed | 148 |
| 93 | SequencerEngine race on rhythm removal during playback — `removeRhythm()` erases from `safePatterns`, `safeCPatterns`, and `lastStepIndex` vectors under `patternLock`, but the audio thread reads those vectors after releasing `patternLock` using a `numRhythms` count snapshotted before the lock. If removal races with the audio iteration loop, the iterator accesses past the shrunken vector end (UB / crash). Was safe with `std::array` (fixed size); exposed by the #76 vector refactor. Fix: revert `safePatterns`, `safeCPatterns`, and `lastStepIndex` to `std::array<…, MaxRhythms>` (audio-thread-only, never resizes) while keeping the message-thread vectors (`rhythms`, `cachedPatterns`, `cachedCPatterns`, `patternUpdated`) as `std::vector`. | ✅ Fixed | 146 |
| 92 | Buffer overrun in ReverbSlot — `wetL`/`wetR` scratch buffers are sized to `blockSize` in `prepare()`, but `process()` and `processReturn()` indexed them with `buffer.getNumSamples()` (no guard). A host that calls with a larger-than-promised block size would write past the end. Fixed by clamping `numSamples` to `wetL.size()` at the top of each function. | ✅ Fixed | 145 |
| 91 | Redundant `std::exp` per channel per block in Insert and Tone filter sections — `OnePoleLP::prepare()` (which computes `exp(−2π·fc/sr)`) was called inside `for (int ch = 0; ch < nCh; ++ch)` in both the bitcrusher anti-alias filter and the post-drive tone filter, computing identical results twice per block. Fixed by hoisting the prepare calls above the channel loop. | ✅ Fixed | 145 |
| 90 | Audio-thread data race in bitcrusher TPDF dither — `VoiceEngine::process()` called `juce::Random::getSystemRandom()` on the audio thread; this is a shared singleton and concurrent calls from multiple VoiceEngine instances race. Fixed by replacing with a per-instance `juce::Random rng` member. | ✅ Fixed | 145 |
| 89 | OneDrive distribution folder structure — create `Windows\` and `Mac\` subfolders under `D:\Andy Tuke\Music - Music Software\TDP\`. Post-build copy hook targets `TDP\Windows\` (was `TDP\`). User manual copies to `TDP\` root. `Mac\` folder is a placeholder for future macOS builds. | ✅ Fixed | 142 |
| 88 | Preset file structure — simplify from full APVTS state dump to a container of 1–8 rhythm subtrees. New root element: `MuClidPreset` with `Rhythm` child nodes each holding per-rhythm APVTS params, name, colour, sample path, and optional embedded sample data. Legacy flat-format files (old `.mu` format) still load via `restoreStateFromTree` fallback. | ✅ Fixed | 142 |
| 87 | Preset file extension — change full-preset extension from `.mu` to `.muclid`. Update all save/load paths in `PluginProcessor`, `PresetBrowser`, and `TransportBar`. Update default preset filename from `_default.mu` to `_default.muclid`. | ✅ Fixed | 142 |
| 86 | User manual — create a comprehensive user manual in `.docx` format covering all plugin features: interface overview, transport bar, rhythm sidebar, rhythm circle, euclidean panel, voice section, modulator panel, mixer, effects (Effect/Delay/Reverb), presets, and settings. Copy to root of OneDrive distribution folder (`D:\Andy Tuke\Music - Music Software\TDP\`). | ✅ Fixed | 142 |
| 85 | Master loop step counter — when the master loop length is set to any value other than ∞, show a small read-only counter adjacent to the loop dropdown displaying the current step position within the master loop (e.g. "12 / 64"). Counter updates at the same rate as the position label (10 Hz timer). Hidden when loop is set to ∞. | ✅ Fixed | 150 |
| 84 | Draw a visible border/panel around the transport bar controls — the transport bar background blends into the window; add a drawn rectangle or outline around the transport section so it reads as a distinct panel, consistent with the bordered panels used elsewhere in the UI. | ✅ Fixed | 146 |
| 83 | Envelope maximum time too long; release should play full sample at maximum — (a) reduce the upper bound of Attack, Decay, and Release knobs from 10 s (100 on the 0–100 scale) to 3 s (maps to the same 0–100 display range); update `adsrTime()` and APVTS param ranges accordingly. (b) When the Release knob is at its maximum value (100 / 3 s), override the normal ADSR release and instead let the sample play to its natural end before cutting off, regardless of sample length. | ✅ Fixed | 146 |
| 82 | Settings overlay close button poorly positioned — the Close button is placed in the top-right corner but overlaps or sits too close to the panel edge; reposition it so it is clearly separated and visually consistent with other overlay close controls. | ✅ Fixed | 146 |
| 81 | Settings overlay — remove the Default BPM / tempo control — the standalone-only BPM input in the Settings overlay is redundant now that the BPM nudge sits in the transport bar. Remove `defaultBpmInput` from `SettingsOverlay`. | ✅ Fixed | 142 |
| 80 | Modulation matrix / modulator panel too short — the "Add destination" button at the bottom of the modulator panel is clipped off-screen at the default window size. The panel height or its scroll area needs to be increased so the button is always reachable. | ✅ Fixed | 141 |
| 79 | Remove the `+` (add rhythm) button from the transport bar — the transport bar has a redundant `+` button alongside the rhythm counter; the sidebar already provides rhythm addition. Remove `addRhythmBtn` and its associated callback from `TransportBar`. | ✅ Fixed | 141 |
| 78 | Amp attack envelope has no effect — the Amp Attack knob on the VoiceSection changes the displayed value but produces no audible change; the voice sounds at full amplitude immediately. Check that `aEnvAtk` is correctly wired through `syncRhythmParam` → `voiceParams.ampEnvAtk` → `VoiceEngine` ADSR; verify the ADSR is applied to the output gain path and that the attack stage is not being skipped or zeroed. | ✅ Fixed | 141 |
| 77 | Modulation of Bitcrusher rate parameter has no effect — assigning a modulator to the Insert Rate destination does not audibly or visibly alter the Bitcrusher sample-rate reduction. Investigate the mod-matrix destination ID for `drvRate`, the `VoiceEngine` modulation read path, and whether the `drvRate` APVTS parameter is correctly mapped through `ModDest` so modulation reaches the DSP. | ✅ Fixed | 141 |
| 76 | Rhythms fixed-size array → dynamic list — `SequencerEngine` stores rhythms as `std::array<Rhythm, 8>` with a `numRhythms` counter, essentially a manual vector. `PluginProcessor` mirrors this with `std::array<VoiceEngine, 8>` (non-copyable, needs `unique_ptr` to move to a vector). APVTS pre-registers all 8 slots regardless so that's not the bottleneck. Main benefit: allows >8 rhythms in the future. Complexity: medium — mechanical but VoiceEngine's non-copyability means `std::vector<std::unique_ptr<VoiceEngine>>` and accessor changes throughout. Not Opus-level; straightforward refactor once the preset system (#68) is in place so we know how state will serialise. | ✅ Fixed | 143 |
| 75 | CLAP plugin format — add a CLAP build target alongside VST3 using the `clap-juce-extensions` third-party module (https://github.com/free-audio/clap-juce-extensions). Add to `CMakeLists.txt` as an additional plugin format. Required for the installer in #71. | ✅ Fixed | 136 |
| 74 | License file system — research and implement: (a) license file format (user name, serial, expiry date, feature flags, cryptographic signature); (b) offline validation using asymmetric signing (e.g. Ed25519 — sign on Andy's machine, verify in plugin with embedded public key — no server required); (c) a small command-line license-generation tool Andy can run locally to issue license files; (d) app reads license file from a well-known path (same folder as content, or `%APPDATA%\TDP\muClid\`) on startup and caches result. Research: `libsodium` or Botan for Ed25519, or a pure-header C++ implementation (monocypher). Implemented: Ed25519 signing via OpenSSL (Git for Windows); Monocypher 4.0.2 for in-plugin verification; `muclid.lic` JSON format with alphabetically-sorted fields and base64 signature; `tools/keygen.ps1` (one-time key generation), `tools/gen_license.ps1` (per-customer), `tools/verify_license.ps1` (support tool); `Source/License/LicenseChecker.h/cpp`; `PluginProcessor` checks license on startup and exposes `isLicensed()`/`licenseInfo`. Private key in `tools/private.pem` (gitignored). | ✅ Fixed | 155 |
| 73 | Demo mode — the same codebase should support a demo build that caps rhythms at 2 and disables all save/export functions. Full features are unlocked by a valid license file (see #74). No separate demo codebase — a single runtime check on license state gates the restricted features. Demo users see a non-dismissable banner or greyed controls for locked features. Implemented: `PluginProcessor::isLicensed()` gates all restrictions; amber `demoBanner` strip above status bar (non-dismissable, 20px, warning colours) when unlicensed; Save button visually dimmed (0.35 alpha) and functionally blocked; rhythm count capped at 2 in demo mode with status bar message on attempted add; `resized()` carves banner height from content area. | ✅ Fixed | 157 |
| 72 | About dialog shows square replacement characters — em dashes and possibly other non-ASCII characters in the credits strings are raw UTF-8 source literals not wrapped in `juce::CharPointer_UTF8()`; on Windows JUCE may interpret them as the system code page. Fix: wrap every string containing non-ASCII with `juce::String(juce::CharPointer_UTF8("..."))`, or replace em dashes with ASCII " - " / " — " via explicit Unicode escapes. | ✅ Fixed | 133 |
| 71 | Installer — build an Inno Setup (or NSIS) installer that: installs the VST3 (to `%ProgramFiles%\Common Files\VST3\`), the CLAP (to `%ProgramFiles%\Common Files\CLAP\`), and the Standalone exe (to a user-chosen path); creates the user content folder from #67 with empty Presets / Rhythms / Samples subfolders; lets the user tick which components to install and change install paths. Installer must be buildable from the CI/CMake workflow. Implemented as `installer/mu-Clid.iss` (Inno Setup 6); CMake target `mu-clid_installer` builds it after a Release build, passing the current build number automatically. Code signing deferred — add `SignTool=` line in the .iss when an EV cert is available. Content folder creation removed from installer (handled by `ensureContentFoldersExist()` on first run). | ✅ Fixed | 152 |
| 70 | Default preset/rhythm on startup — on fresh load, the plugin reads `_default.mu` from the user Presets folder and restores it silently. When a new rhythm is added, `_default.muRhyth` is read from the Rhythms folder and applied. Users can save over either file to create a personal default. If either file is missing, an empty item is created with no error shown. | ✅ Fixed | 140 |
| 69 | Sample embedding in preset files — the Save dialog should offer a checkbox "Embed samples". When checked, all referenced sample files are base64-encoded and stored as child nodes inside the `.mu` XML so the preset is fully self-contained. On load, embedded samples are extracted to a temp directory and loaded from there. Non-embedded presets reference samples by absolute path with a fallback search in the user Samples folder. | ✅ Fixed | 140 |
| 68 | Preset and rhythm file formats — full presets use extension `.mu`, rhythm presets use `.muRhyth`; both are JUCE ValueTree serialised to XML. Implement save/load wired to PresetBrowser / SaveDialog. File is written to the user content folder from #67. Category metadata stored as a ValueTree property (see #54). | ✅ Fixed | 140 |
| 67 | Content folder structure — Windows default path `%USERPROFILE%\Documents\TDP\muClid` with three subfolders: `Presets\`, `Rhythms\`, `Samples\`. Path should be user-changeable in the Settings overlay. Folder is created on first launch if it does not exist. Settings paths persist in `juce::ApplicationProperties`. | ✅ Fixed | 140 |
| 66 | RhythmCircle rings lack a visible loop-point marker — users cannot see where the ring wraps from step N back to step 0; add a radial line between the last and first step in a colour slightly brighter than the panel background | ✅ Fixed | 128 |
| 65 | Filter cutoff display should show Hz below 1 kHz and kHz above — e.g. "440 Hz", "1.0 kHz", "8.5 kHz", "20.0 kHz"; underlying value always in Hz | ✅ Fixed | 128 |
| 64 | Double-clicking a knob in VoiceSection should open an inline text editor for manual value entry — previously double-click reset to minimum; now shows a `TextEditor` overlay over the value dead zone, accepting typed values (with `textFromValueFunction` / `valueFromTextFunction` round-trip) | ✅ Fixed | 128 |
| 63 | Insert Bitcrusher second knob should be labelled "Rate" (not "Output") in Bitcrusher mode, with range 100–48000 Hz and Hz/kHz formatting — the knob previously showed "Output" (−24..0 dB) in all modes; in Bit mode it now dynamically switches label, range, formatter, and APVTS routing to `drvRate` | ✅ Fixed | 128 |
| 62 | Drive "Tone" label should read "LPF", and the same 1-pole LP filter code should be used across all insert modes (Soft / Hard / Fold / Bitcrusher) — currently "Tone" was a placeholder label and the filter implementation was an inline IIR; extracted into reusable `OnePoleLP` struct in `AudioFilters.h` | ✅ Fixed | 128 |
| 61 | Drive dropdown label should say "Bitcrusher" not "Bit" — the drive algorithm dropdown uses "Bit" as the display name for the bitcrusher algorithm; rename to "Bitcrusher" to match user expectations | ✅ Fixed | 128 |
| 60 | Insert panel label should read "INSERT" not "DRIVE" — the fourth column of the VoiceSection (Pitch / Filter / Amp / Drive) is named "DRIVE" in the painted section header; rename to "INSERT" to match the insert-effect concept and the user-facing terminology used elsewhere | ✅ Fixed | 128 |
| 59 | Envelope time displays should adapt — Pitch / Filter / Amp envelope ADSR knobs should show times in milliseconds (0–999 as integer "0 ms" – "999 ms"), then switch to seconds at the 1 s boundary ("1.0 s" – "9.9 s" with one decimal). Maximum 10 s. Matches the existing Hz→kHz convention used on the filter cutoff display. Implementation: a shared `formatTimeFromMs(double ms)` helper, applied via `textFromValueFunction` on each envelope knob in `VoiceSection` (and `valueFromTextFunction` for round-trip parsing) | ✅ Fixed | 127 |
| 58 | Drive insert effect causes master redlining — the Drive stage (Soft / Hard / Fold / Bit) can output significantly louder than its input depending on the Drive amount; needs proper input-to-output gain staging (auto-gain compensation, or an explicit Output knob whose default is calibrated to keep RMS roughly constant at 50 % drive). Currently the Output knob exists (−24..0 dB) but the user has to manually compensate for any drive boost, and the Stage 19/20 headroom budget assumes the Drive doesn't add gain. Fix: build per-algorithm gain-compensation curves (measured) so that driving the input doesn't increase the perceived RMS of the output. Tracked separately from the Stage 19/20 mixer headroom work because this is per-algorithm DSP, not gain trim | ✅ Fixed | 136 |
| 57 | Bitcrusher "Rate" knob displays an abstract number (e.g. 24.0) — should display the actual target sample rate the bitcrusher is reducing to, e.g. "44100 Hz" or "44.1 kHz". Implementation: when `driveChar == Bit`, override the Output knob's `textFromValueFunction` to compute `currentSampleRate / divisor` from the parameter value and format with the existing Hz→kHz convention (0–999 Hz integer, 1.0–9.99 kHz two decimals, ≥10 kHz one decimal — matches filter cutoff display) | ✅ Fixed | 128 |
| 56 | Bitcrusher "Bits" knob displays the wrong unit — currently shows the raw 0–100 parameter value; should display the actual target bit depth as an integer 1–16. Implementation: when `driveChar == Bit`, override the Drive knob's `textFromValueFunction` to return `juce::String((int)juce::jmap(value, 0.0, 100.0, 16.0, 1.0))` (or whatever mapping the algorithm uses internally), with units " bits" | ✅ Fixed | 127 |
| 55 | Drive (insert effect) algorithm dropdown lacks a "None" option — the per-rhythm Drive section on the sequencer page only offers Soft / Hard / Fold / Bit; users must turn `Drive` to 0 to effectively bypass it. Add a "None" entry as the new index 0 (shifting the others down) and short-circuit the Drive stage in `VoiceEngine` when `driveChar == None` so it adds no CPU cost and produces bit-identical output to bypass. Update `VoiceParams::driveChar` range and the Drive label-update logic that currently keys on Bitcrusher (3 → 4) | ✅ Fixed | 127 |
| 54 | Preset browser is too small and lacks organisation — (a) widen the preset browser dropdown / panel so longer preset names aren't truncated; (b) add a Category field to each preset (e.g. "Drums", "Bass", "Ambient", user-defined). The Save dialog gets a category combo-box that lists existing categories with a "New…" option to add a new one. The browser groups presets by category (collapsible headers or a category column). Persist categories as a property in the `.mu` preset file (see design-future.md preset format) | ✅ Fixed | 136 |
| 53 | TransportBar "+" icon next to Save does nothing — there is a button rendered as a "+" adjacent to the Save button on the transport bar that has no `onClick` action wired up; either remove the button or hook it up to an actual function (likely intended as "save as new preset" or "create new preset slot") | ✅ Fixed | 133 |
| 52 | TransportBar Master Loop layout — relocate the loop dropdown so it sits inside the transport panel itself (alongside play/stop/BPM) rather than floating separately; group it visually as part of the transport, ideally as a labelled sub-panel "Master Loop". Resolves #51 at the same time | ✅ Fixed | 133 |
| 51 | TransportBar loop dropdown lacks a label — the "∞ / 16 steps / 32 steps / …" dropdown sits unlabelled in the transport bar; add a "Master Loop Length" label adjacent to it so its purpose is clear (consistent with other labelled controls in the bar) | ✅ Fixed | 127 |
| 50 | Audio-thread safety: `SequencerEngine::cachedPatterns`/`cachedCPatterns` read in audio thread without lock — `processBlock` reads `cachedPatterns[r]` (a `std::vector<bool>`) while `updatePattern()` on the message thread can reassign it. The reallocation could leave the audio thread reading freed memory. Pre-existing design. Fix options: (a) double/triple-buffer the cached patterns with atomic index swap; (b) protect with the `modLock` spinlock; (c) use a lock-free SPSC queue to publish updated patterns to the audio thread | ✅ Fixed | 138 |
| 49 | Audio-thread safety: `Rhythm::operator=` is not lock-aware — `RhythmPanel::confirmReset` does `r = Rhythm{}` which replaces `controlSequences` (`std::vector<ControlSequence>`) and `modulationMatrix` non-atomically while the audio thread may be reading them. The `modLock` spinlock guards modulation matrix reads in `processBlock` but the assignment bypasses it. Same exposure on `addRhythm`. Practical risk: brief audible artefact (not crash) during reset. Fix options: (a) acquire `modLock` around the assignment in `confirmReset`/`addRhythm`; (b) make `Rhythm::operator=` itself lock-aware so any caller is safe | ✅ Fixed | 138 |
| 48 | Euclid Pad/Mute toggle text truncated — the segment toggle is too narrow to display the full "Pad"/"Mute" labels; widen the toggle so the full text is visible without ellipsis | ✅ Fixed | 124 |
| 47 | Euclid B rotate control has no audible/visual effect — investigate Rhythm.genB rotation wiring; rotation works on Euclid A but not B | ✅ Fixed | 127 |
| 46 | Euclid B — one of the steps occasionally renders in the wrong colour; intermittent visual bug, repro steps unknown — likely a stale state-type entry in the cached step pattern or an off-by-one in `RhythmCircle::setPatterns` | ✅ Fixed | 136 |
| 45 | TransportBar Loop dropdown — when Loop is Off (= reset length for the whole pattern), display an infinity symbol (∞) instead of the literal text "Loop Off" | ✅ Fixed | 124 |
| 44 | Mixer Effect (EFX) return channel has a Mix knob — Effect is a send/return path, so the return should be wet-only; remove the Mix knob from the Effect row in MixerOverlay (Delay and Reverb returns same convention) | ✅ Fixed | 124 |
| 43 | Sidebar item does not pulse on hit — the expanding pulse ring centred on the miniCircle (introduced for #39) is no longer firing; pulse should be a circle drawn in the centre of the sidebar item when its rhythm fires | ✅ Fixed | 124 |
| 42 | Pre/Post pad mode toggles mislabelled — "Ignore" label should match the insert section convention and read "Pad" instead | ✅ Fixed | 110 |
| 41 | Drive section layout — algorithm dropdown on row 1 alongside controls; controls should be on row 2 with the dropdown spanning the full section width on row 1 | ✅ Fixed | 110 |
| 40 | Drive algorithm labels static — "Drive / Output / Tone" labels on the Drive controls do not update when the algorithm changes; Bitcrusher should show "Bits / Rate / Tone" | ✅ Fixed | 110 |
| 39 | Sidebar item flashes entire background on hit — should show a small expanding pulse ring centred on the miniCircle instead of a full background colour flood | ✅ Fixed | 110 |
| 38 | Mixer channel level defaults too high — default channel level 0.75 (−2.5 dB) causes loud samples to peak near the top of the VU meter at rest; default lowered to 0.5 (−6 dB) | ✅ Fixed | 110 |
| 37 | Flanger rate range too narrow — 0.1 Hz minimum still produces noticeable modulation; 8 Hz maximum is not fast enough for jet-flange effect | ✅ Fixed | 110 |
| 36 | Mixer VU meters read pre-fader — master, Effect, Delay, and Reverb return VUs captured before their respective gain stages so faders had no effect on the meters; inactive channel peaks not cleared when rhythms removed | ✅ Fixed | 108 |
| 35 | Sequencer page modulation section dropdowns too wide — the Step and Loop dropdowns in the modulator timing row are over-sized; shrink to fit their content and left-align all controls on that row | ✅ Fixed | 109 |
| 34 | Sequencer page master loop dropdown too narrow — the TransportBar loop/length dropdown clips its text; it should be wide enough to always display the full selected option without truncation | ✅ Fixed | 109 |
| 33 | Sequencer page Pitch/Filter envelope depth — the Depth knob currently sits in the envelope (bottom) row; move it to the config (top) row for both Pitch and Filter columns so the envelope row only contains ADSR | ✅ Fixed | 110 |
| 32 | Mixer page FX section panel — the three effect rows (Effect/Echo, Delay, Reverb) should sit inside a dedicated container panel, with each row inside its own sub-panel | ✅ Fixed | 113 |
| 31 | Mixer page FX knob sizing — Reverb row knobs are larger than Effect and Delay row knobs; all three rows must use the same knob size and be vertically aligned with each other | ✅ Fixed | 113 |
| 30 | Bitcrusher: add pre-filter anti-aliasing and TPDF dither — add LP at new Nyquist before sample-hold, add triangular dither `x + (r1-r2)*lsb` before quantisation | ✅ Fixed | 112 |
| 29 | Drive/Waveshaper: implement ADAA (Antiderivative Anti-Aliasing) — current `tanh` aliases without oversampling; ADAA formula: `(ln(cosh(x[n])) - ln(cosh(x[n-1]))) / (x[n] - x[n-1])` gives 8–16× equivalent at near-zero CPU cost | ✅ Fixed | 112 |
| 28 | Chorus: upgrade from linear to Hermite cubic interpolation, add per-voice LFO rate detuning (±0.5–2%) for organic evolution vs. fixed-period modulation | ✅ Fixed | 112 |
| 27 | Phaser: fix LFO-to-allpass-coefficient mapping — current linear sweep clusters notches at high frequencies; correct mapping: convert LFO → Hz, then `a = (1 - tan(π*f/sr)) / (1 + tan(π*f/sr))` | ✅ Fixed | 109 |
| 26 | Flanger: implement through-zero flanging — current min delay is 0.5ms so the zero-crossing never occurs; fix: delay dry signal by `baseSamp`, sweep wet path from 0 to `2*baseSamp` | ✅ Fixed | 109 |
| 25 | Delay: add fractional interpolation and smooth delay time changes — current integer read pointer produces clicks/artefacts on BPM change or LFO modulation; Hermite cubic interpolation + 50ms parameter smoothing required | ✅ Fixed | 109 |
| 24 | Reverb: replace `juce::Reverb` (Freeverb/Schroeder) with Signalsmith FDN reverb — Freeverb produces metallic flutter on percussive sources; Signalsmith uses rotated-Hadamard FDN with per-line modulation | ✅ Fixed | 105 |
| 23 | Settings Overlay controls not audited — review all panels (Visual, Sequencer, Performance, Voice, Gain, Presets, Standalone) against design | ✅ Audited | 103 |
| 22 | Intra-FX return send knobs (Effect→Delay, Effect→Reverb, Delay→Reverb) — wiring to APVTS params `"eff2dly"`, `"eff2rev"`, `"dly2rev"` not verified end-to-end | ✅ Verified | 103 |
| 21 | TransportBar Loop/Sync dropdown — verified: current dropdown correctly controls master loop length (`mstrLoop` param, "Loop Off" / "N steps"). Design intent "Host Sync / Reset on Play / Free Running" describes a separate sync mode control not yet implemented — deferred to a later stage | ✅ Design resolved | 103 |
| 20 | TimeSelector component in `Source/UI/Components/` appears to be dead code (replaced by DropdownSelect) — confirm unused and remove | ✅ Fixed | 103 |
| 19 | Euclid C accent controls — design resolved: accent = Ring C coincidence with A+B hit, boosted by `accentDb` knob in Amp panel. Advance Mode and Accent Velocity descoped from v1. Tracked under #14. | ✅ Design resolved | 82 |
| 18 | Euclid C padding controls decision — resolved: design updated to include full A/B padding set on C | ✅ Design resolved | 82 |
| 17 | Amp FX send knobs (Effect, Delay, Reverb) on the Voice Amp column row not implemented — specified in Stage 10 design | ✅ Fixed | 103 |
| 16 | VoiceSection status bar labels use abbreviations throughout — "Pitch Atk/Dec/Sus/Rel", "Filt Env Atk/Dec/Sus/Rel/Dep", "Amp Atk/Dec/Sus/Rel", "Filter Res", "Drive Out" | ✅ Fixed | 103 |
| 15 | Label abbreviations violate "full words everywhere" design principle — `"Res"` → `"Resonance"`, `"Semi"` → `"Semitone"`, segment modes `"I"/"M"` → `"Ignore"/"Mute"` / `"Pad"/"Mute"`, Delay modifiers `"Str"/"Dot"/"Tri"` → `"Straight"/"Dotted"/"Triplet"` | ✅ Fixed | 103 |
| 14 | Accent system not implemented — Ring C coincidence detection missing in SequencerEngine; `isAccented` flag not passed to VoiceEngine; Accent knob (0–+12 dB) not present in VoiceSection Amp config row; `accentDb` APVTS param not wired | ✅ Fixed | 104 |
| 13 | Euclid C padding/insert controls — design updated; C intentionally shares the full A/B parameter set | ✅ Design resolved | 82 |
| 12 | Rhythm rename does not propagate — name updates in panel header but not sidebar item or control bar; switching rhythms shows stale name in header | ✅ Fixed | 114 |
| 11 | VU meters are very slow to respond | ✅ Fixed | 82 |
| 10 | High-pass filter produces no sound when resonance is set to 0 | ✅ Fixed | ≤81 |
| 9 | Accent ring (Ring C) steps are drawn as lots of lines instead of filled blocks | ✅ Fixed | 82 |
| 8 | On the sequencer page, the drive insert effect doesn't do anything | ✅ Fixed | ≤81 |
| 7 | Hits should fire when the step passes the 12 o'clock position | ✅ Fixed | ≤80 |
| 6 | EFX output is very loud — gain staging needs to be corrected to produce a usable output level | ✅ Fixed | ≤80 |
| 5 | EFX knob ranges are not user-friendly (e.g. Resonance shows 0–1 instead of a meaningful scale) | ✅ Fixed | ≤80 |
| 4 | Delay feedback knob range should be 0–100 (not 0–0.95) | ✅ Fixed | ≤80 |
| 3 | While playing, turning the Steps knob in Euclid B triggers the sample | ✅ Fixed | ≤80 |
| 2 | Delay feedback does not appear to work | ✅ Fixed | ≤80 |
| 1 | Delay time — both sync and free modes do not appear to work | ✅ Fixed | ≤80 |
