Read backlog.md to find the current highest issue number, then add a new Open issue at the top of the `## 🔴 Open` section for the following feature request: $ARGUMENTS

Rules:
- Row format: `| <next number> | <description> | 🔴 Open | — |`
- The new issue number must be one higher than the current highest number anywhere in backlog.md
- Insert at the top of the Open section (highest number goes first — descending order)
- One discrete issue per row; if the request has multiple independent parts, split into separate numbered rows — use the next sequential whole numbers (e.g. if 283 is the highest, use 284, 285, …). Never use suffix notation like #282a, #282b
- Do not alter any other content
- Do not ask for confirmation
